---
kind: rks-adversarial-challenges
target:
  task: publish-split-packages-0-8
  revision: task-01-round-4-task-03-round-11-complete-tree
round: 8
challenger: release-change-challenge-r8-20260810
---

# Adversarial Challenges: TASK-01 Round 4 / TASK-03 Round 11 complete release tree

- Target: complete unstaged result at baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; workflow `sha256:4faf39ec30d67156b70c0d0e3e4d66aad7e956731c144ed67b067dc9e694ddcf`, canonical contract `sha256:de2384d69df8c1f82e253da75cab4ee932b45bc17d822e239fbe5352780b97ad`, generator `sha256:c90a37b383f2fef77c2f83e2e02a40d3a573b6a454263b42924aa70db52fd948`, verifier `sha256:39cf1ff2443b20f50ecc4ee60d8e50bb83227cfa9ccea33b0fb8504aa8be3219`, eligibility `sha256:9cc5e1550f2d7f294c79fb77087cdd1d540ad7f4c04e1623ec78bd998925f197`, publisher `sha256:1636a4ca7c988e6971265eeab83288dedad0079d91c3b0efad3dc162675a7da0`, Registry smoke `sha256:617dd31d162906a90de701db6e4e77b3e85d5c09debaef0e9bd98023b0dc0bb2`, release tests `sha256:74cb1296035f179aae9400f36b2b3df6da343e9c3ff78a75a51b2d7d20255783`, and generated manifest `sha256:4f1774dc44b04a74f4e44c25002f4948da024fbfbbdfb79f567a1b411f9ce2bc`.
- Governing sources: `../TaskContract.md` revision 1 (`sha256:63e182e672ef81360cd939befa4247ad92af291fe9378547ad8d442472d947a6`); `../spec.md` ready (`sha256:048fc512a081dd310e5dcf028854b9b5bcc36fa0b4d20c1158079eab494390d9`); `../plan.md` approved (`sha256:c3f02c2ab099f7cf15dd1d1c9ee7fecec78068a7a0c7f597e639f2170ed6a33f`); completed Task registry; latest TASK-03 Round 11 execution/self-check/quick-review evidence; and `./drift-review.md` aligned Round 9 (`sha256:67c87a4f36e6841d28f84e988faa5171262c8b719b94753cd04644cb9d07252f`).
- Attack scope: immutable external-Action selection, exact allowlist integrity, SHA/comment consistency, Action compromise and permission boundaries, retained-artifact open inventory, package/repository identity, pull-request and one-shot eligibility semantics, OIDC/npm-token confinement, npmjs-only publication, exact accepted tarballs, core-first and partial-release recovery, provenance statement binding, Registry-only consumer smoke, and exact three-package scope. Safe probes only: current sources and candidate inventory were inspected; all external Action references were enumerated; credential-free `git ls-remote` compared each selected SHA with the official repository's current `refs/tags/v4`; the complete pinned release regression ran locally; and temporary candidate copies reproduced the open-inventory behavior. No credential was accessed, no GitHub workflow/artifact operation or npm Registry request occurred, and no implementation, workflow state, Git state, commit, push, publication, or other external mutation was performed.

## Challenge CH-1

- Target claim or boundary: The retained pre-mutation artifact is a complete, non-secret, closed representation of the exact accepted release candidate (TaskContract credential and exact-candidate constraints; Spec RQ-015/RQ-017; Plan T-005 step 4, T-008, CK-010/CK-017).
- Attack: Add an undeclared regular file such as `unexpected.txt`, `.env.production`, or `credentials.json` beside `.release/candidate/manifest.json`, or add a symlink named `.npmrc` to an external file, after candidate generation but before upload. The verifier closes each declared package tree and verifies the three declared archives, but does not close the candidate root. The pre-upload residue command inspects only regular files named `.npmrc`, `*.pem`, or `*.key`; with `include-hidden-files: true`, the hosted upload selects the entire candidate tree.
- Failure mechanism: An undeclared candidate-root regular file does not contribute to the joint candidate identity or any package integrity and passes `release:verify --policy-only`. A symlink is not selected by the residue guard's `find ... -type f`. Its hosted handling is left to the pinned upload Action rather than an asserted candidate policy. The publish-side verifier also ignores undeclared root entries after download.
- Task outcome at risk: Retention should not copy runner configuration, credentials, or unaccepted residue into the release artifact used for same-run recovery and closeout.
- Credible scenario: An unexpected build diagnostic, future tool change, or concurrent runner process writes one extra root file or link between deterministic generation/reacceptance and the upload. The present generator reconstructs the exact candidate directory and the current candidate contains no such entry, so this requires an additional producer or a narrow race rather than occurring on the demonstrated normal path.
- Implementation consequence: CI may retain a file that was neither hashed nor inspected. Sensitive residue could become available to principals able to download the run artifact; nonsensitive residue makes the retained candidate inventory open rather than exact. The declared tarballs remain independently integrity-checked before publication.
- Downstream impact: The bounded consequence is retained-artifact scope drift and a contingent confidentiality leak. It does not provide a demonstrated path to publish altered package bytes because the three archives, manifests, paths, SRI values, source revision, and common candidate identity are reverified immediately before mutation.
- Why now: A closed allowlist immediately before the hidden-file upload would remove the gap before the first hosted run. However, no current producer or current residue was demonstrated, and the retained artifact can be inspected/downloaded during closeout without changing immutable npm bytes, so this does not require holding the release gate open.
- Expected or required behavior: Ideally the pre-upload boundary permits only `manifest.json`, `run-record.json`, the three manifest-declared archives, and the manifest-declared package files, while rejecting every symlink, special entry, and undeclared candidate-root item.
- Evidence or probe: Under Node `22.22.0` and npm `10.9.4`, a temporary copy with `unexpected.txt` beside its manifest made `node scripts/verify-release-packages.mjs --candidate <copy>/manifest.json --policy-only` exit `0`; a separate temporary copy with `.npmrc -> /etc/hosts` made the workflow's exact `find ... -type f` residue predicate exit `0`. The real candidate contains no symlink and its root contains only `archives/`, `manifest.json`, and `packages/` before CI adds `run-record.json`.
- Source anchor: TaskContract constraints and SC-3/SC-7; Spec RQ-015/RQ-017, AC-004/AC-011; Plan BD-002/BD-006/BD-008, T-005 step 4, T-008 steps 1-3, CK-010/CK-017, and the non-secret retained-candidate boundary.
- Risk: low-risk
- Minimum mitigation: None required for this gate; if hardened, derive a closed candidate-root allowlist from the accepted manifest plus `run-record.json` and reject all symlinks/special entries immediately before upload.
- Risk basis: The protected retention/confidentiality boundary can be affected, but the credible scenario needs an unobserved extra producer or race, the current candidate is clean, package bytes stay independently verified, and artifact inspection provides cheap detection; the credible-scenario and why-now conditions for high-risk are therefore incomplete.

## Probe disposition outside retained challenges

- Action selector integrity: all 13 external `uses:` values are exact lowercase 40-hex commits and belong only to `actions/checkout`, `actions/setup-node`, `actions/upload-artifact`, or `actions/download-artifact`. Credential-free official ref probes on 2026-08-10 resolved `refs/tags/v4` to the exact selected commits: checkout `11d5960a326750d5838078e36cf38b85af677262`, setup-node `49933ea5288caeca8642d1e84afbd3f7d6820020`, upload-artifact `ea165f8d65b6e75b540449e92b4886f43607fa02`, and download-artifact `d3f86a106a0bac45b974a628896c90dbdf5c8093`. Every trailing `# v4` comment therefore matches its selected official v4 commit now. The comments do not influence YAML execution or allowlist acceptance, so a future stale comment would be a maintenance defect rather than an artifact-substitution path; no distinct challenge is retained.
- Action compromise boundary: full commit selection prevents later tag movement from changing executed bytes. A compromise that had already inserted hostile bytes into an official repository before this exact SHA was selected remains a general upstream trust risk, but current evidence identifies the official repositories and current official v4 objects, and no source-grounded project control beyond reviewed content-addressed selection was required. No credible current-tree path was demonstrated in which a moved tag, unapproved owner, short SHA, or syntactically valid substitute passes the exact allowlist.
- Workflow-policy boundary: the regression enumerates every current non-local step-level Action, requires exactly 13 references, and rejects a tag, branch, short SHA, and unapproved full-SHA owner. The current workflow has no reusable job-level `uses:` or local Action. A hypothetical future job-level Action would require a new Git diff and fresh review; it does not create a failure in this exact accepted tree.
- Repository/package and pull-request outcomes: root `0.8.0` is private, the legacy aggregate/CLI has no publication route, package generation targets exactly core/engine/extensions, dependency direction is `engine/extensions -> core@0.8.0`, and all declared roots/subpaths are covered. Pull requests run validation and eligibility only; eligibility returns false outside an exact master push whose checked-out full-history SHA uniquely adds the exact intent marker. Candidate retention and publication remain downstream of the full repository/package/eligibility gates.
- Permission and credential outcomes: global permission is `contents: read`; retained-candidate access adds only `actions: read`; publish adds `id-token: write`; and the only `secrets.NPM_TOKEN` reference is step-scoped as `NODE_AUTH_TOKEN` on the exact publisher command. All preceding external components are now fixed to the reviewed allowlist. Publisher and consumer npm subprocesses use isolated configuration, enforce npmjs, and scrub unrelated credential/config inheritance. No PR-runnable job has artifact upload, publication, npm secret, or OIDC authority.
- Publication and recovery outcomes: the publisher consumes only canonical contained retained `.tgz` paths for the fixed three-package allowlist, recognizes absence only from authoritative npmjs 404, compares present SRI/source/common candidate identity, rejects foreign or invalid mixed states, publishes core first, waits for core visibility, and resolves ambiguous command outcomes only by exact Registry identity. Fixed non-cancelling concurrency prevents interleaving eligible 0.8.0 mutations, and same-run artifact recovery is reaccepted before reuse.
- Provenance and smoke outcomes: Registry smoke is credential-free and npmjs-only, binds every retained SHA-512 subject, source repository/revision, and common candidate identity to one structurally valid supported Sigstore/SLSA statement, explicitly reports that standalone cryptographic verification is not performed, then installs only engine/extensions with strict peers and proves automatic core, one compatible Zod, and all 12 runtime/12 TypeScript entries. No separate material path was demonstrated that would let foreign metadata, a cross-origin endpoint, an ambiguous/multiple attestation, malformed certificate chain, or mismatched subject/source pass.
- Fresh regression evidence: under Node `22.22.0` and npm `10.9.4`, `env -u NODE_AUTH_TOKEN npm run release:test` passed all `14/14` cases in `30.42s`, including deterministic package projection, hostile configuration isolation, provenance binding, partial/ambiguous publication states, complete-history eligibility, npmjs confinement, and the full pinned workflow policy. `git diff --check` and the empty-staging assertion passed. No high-risk failure survived the outcome-focused probes.
- This artifact is an attack record, not a review verdict or completeness proof.
