# TASK-02 — Verify packed consumers and document the split

## Task Unit Contract

- Contract revision: 1
- Goal and deliverables: Implement fail-closed candidate verification and clean-consumer runtime/type smoke tests for every declared entry, plus accurate English and Chinese installation/import guidance.
- Referenced spec requirements: RQ-001, RQ-002, RQ-003, RQ-004, RQ-005, RQ-006, RQ-007, RQ-012, RQ-013, RQ-016, RQ-017.
- Referenced plan sections: T-002, T-003 step 3; BD-002, BD-005; CK-003, CK-004, CK-006.
- Owned boundary: Candidate identity/inventory/export/dependency/peer/reference verification, isolated tarball installation, twelve runtime and TypeScript entry probes, negative package-policy fixtures, and directly related bilingual consumer documentation.
- Permitted files or interfaces: `scripts/verify-release-packages.mjs`, verification/consumer-focused cases in `scripts/release-packages.test.mjs`, supporting non-secret fixtures under `scripts/fixtures/`, release verification npm scripts in `package.json`, `README.md`, `README_CN.md`, and directly related `document/**` import examples when required.
- Dependencies: TASK-01.
- Required dependency outputs: Canonical package contract, generator, root 0.8.0/private metadata, and exact frozen candidate format from TASK-01.
- Acceptance conditions: The verifier rejects changed archives, invalid manifests/exports/dependency ranges/peers, forbidden content/layers/references, horizontal MiniAgent dependencies, and unaccepted candidates; a clean strict-peer project installs exact tarballs, observes automatic core installation and one compatible Zod resolution, and resolves all twelve entries at runtime and through strict NodeNext TypeScript; docs use new package names and explain automatic core installation with no 0.8 aggregate/CLI route.
- Task-specific authorization: Root TaskContract authorization for local implementation, temporary consumer directories, and public read-only npm metadata only; no commit, push, npm credential access, or Registry mutation.
- Required methods: Apply TDD to verifier failure behavior and clean-consumer acceptance; use `mkdtemp`-style contained temporary directories and remove only exact owned temporaries; never read local npm credentials.
- Required checks: Focused release policy tests; full candidate verification against TASK-01 output; documentation assertions; `npm run typecheck`; `git diff --check` for owned files.
- Required evidence: Appended changed paths, RED/GREEN output, rejected negative cases, clean dependency tree summary, twelve runtime/type results, documentation evidence, executor self-check, and matching passed independent quick-review.

## Execution Result Round 1 — executor-task02-20260809

- Implemented fail-closed frozen-candidate verification in `scripts/verify-release-packages.mjs`: exact package/marker/manifest/export/dependency policy, no peer or CLI leakage, safe candidate paths, disk/`npm pack --json --dry-run`/tar inventory agreement, SHA-512 archive identity, extracted archive byte agreement, generated-tree candidate-ID recomputation, forbidden layer/reference/source-map checks, and exact export-target checks. Added `release:verify` to `package.json`.
- Implemented a repository-isolated clean consumer under an `os.tmpdir()` `mkdtemp` directory, exact three-tarball `npm install --strict-peer-deps --ignore-scripts`, complete `npm ls --all --json`, non-symlink containment checks, MiniAgent dependency-direction assertions, one physical Zod resolution, twelve runtime imports, and twelve strict NodeNext TypeScript resolutions. Exact owned temporaries are removed in `finally`; no repository link is created.
- Updated `README.md`, `README_CN.md`, and the directly related `document/tools/**` import examples to the core/engine/extensions package names. Both readmes explain automatic compatible-core installation and explicitly state that 0.8 publishes neither the old aggregate nor a CLI package.
- Changed paths: `scripts/verify-release-packages.mjs`, the verification/consumer/docs cases in `scripts/release-packages.test.mjs`, `package.json`, `README.md`, `README_CN.md`, `document/tools/agent-context.md`, `document/tools/agent-context_CN.md`, `document/tools/bash.md`, `document/tools/bash_CN.md`, `document/tools/edit.md`, `document/tools/edit_CN.md`, `document/tools/glob.md`, `document/tools/glob_CN.md`, `document/tools/grep.md`, `document/tools/grep_CN.md`, `document/tools/mcp.md`, `document/tools/mcp_CN.md`, `document/tools/read.md`, `document/tools/read_CN.md`, `document/tools/skill.md`, `document/tools/skill_CN.md`, `document/tools/subagent.md`, `document/tools/subagent_CN.md`, `document/tools/todo.md`, `document/tools/todo_CN.md`, `document/tools/write.md`, `document/tools/write_CN.md`, and this Task evidence file. TASK-01-owned generator/contract/root lock changes were reused rather than reimplemented.
- TDD RED: `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `1`; decisive output: generator test passed, then `accepts only the exact frozen candidate package policy` failed with `Cannot find module .../scripts/verify-release-packages.mjs`, `tests 2`, `pass 1`, `fail 1`. The failure demonstrated absent verifier behavior.
- TDD GREEN: `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `0`; decisive output after the minimum verifier policy implementation: `tests 2`, `pass 2`, `fail 0`. The subsequent docs case is included in the final focused result below.
- Rejected negative cases: changed archive bytes/SRI; manifest inventory containing a forbidden test layer; missing export targets; wrong internal core range; a horizontal engine-to-extensions dependency; peer leakage; an escaping relative-core reference/unrecorded artifact; and a mismatched/unaccepted joint candidate marker. Each fixture is copied to a unique repository-external temporary candidate and removed afterward.
- Final CK-003/CK-006 focused command: `fnm exec --using=22.22.0 npm run release:test -- --docs` exited `0`; decisive output: deterministic generator, exact candidate policy/negative cases, and bilingual split-route documentation all passed (`tests 3`, `pass 3`, `fail 0`).
- Final CK-004 command: `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` exited `0`. The verified regenerated frozen candidate is `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories remain core 63 files/1 entry, engine 103 files/7 entries, and extensions 91 files/4 entries. The candidate identity changed from TASK-01 only because its approved packaged `README.md` content now contains the TASK-02 consumer guidance; the TASK-01 generator, canonical contract, source revision, package structure, and exact identity rules were reused unchanged.
- Clean dependency/peer result: strict-peer installation and `npm ls --all --json` passed; engine and extensions each resolve the ordinary `@piaoxianguo/miniagent-core@0.8.0` edge, neither has a horizontal sibling edge, core has no MiniAgent dependency, all SDK peer constraints are satisfied by strict npm installation, and all three packages resolve the same physical `zod@3.25.76` installation.
- Twelve runtime and strict NodeNext type results, each passed in both modes: `@piaoxianguo/miniagent-core`; `@piaoxianguo/miniagent-engine`; `@piaoxianguo/miniagent-engine/anthropic`; `@piaoxianguo/miniagent-engine/openai`; `@piaoxianguo/miniagent-engine/openai-compatible`; `@piaoxianguo/miniagent-engine/glm`; `@piaoxianguo/miniagent-engine/glm-codeplan`; `@piaoxianguo/miniagent-engine/nvidia`; `@piaoxianguo/miniagent-extensions`; `@piaoxianguo/miniagent-extensions/mcp`; `@piaoxianguo/miniagent-extensions/skill`; `@piaoxianguo/miniagent-extensions/subagent`.
- Documentation evidence: the focused docs test scans both readmes and all directly related `document/tools/*.md` imports against the twelve-entry allowlist, rejects old aggregate/subpath imports and aggregate install commands, requires all three split package names, automatic-core wording, and the explicit no-aggregate/no-CLI 0.8 statement.
- Required type check: `fnm exec --using=22.22.0 npm run typecheck` exited `0`; decisive output: `tsc --noEmit` completed without diagnostics.
- Recovered execution exception: the first full verifier run exited `1` because macOS exposes the same temporary directory as logical `/var` and canonical `/private/var`; a focused `mktemp`/`realpath` diagnostic confirmed the alias. Containment now compares canonical roots, after which the identical full verifier passed; this changed no package or consumer result.

## Executor Self-check Round 1 — executor-task02-20260809

- Scope: implementation is confined to TASK-02-permitted verifier/tests/npm script/bilingual consumer documentation and this evidence. It did not alter `src/`, the TASK-01 generator/contract, SDD controller state, Git history, remotes, Registry state, workflows, publication logic, credentials, or tokens.
- Fail-closed contract: the verifier derives the exact twelve entries and three manifests from the canonical TASK-01 contract; requires one common recomputed candidate identity/source marker; compares package trees, packed inventories, archive identities, extracted bytes, exports, dependencies and references; rejects peers, horizontal MiniAgent dependencies, aggregate/CLI metadata, wrong layers, unsafe paths, and changed archives.
- Consumer isolation: every clean-consumer and archive-extraction directory is created outside the repository, has a unique owned prefix, receives exact frozen tarball paths, has no MiniAgent symlink or repository-resolving package, and is removed only by its enclosing `finally`. Network use is limited to ordinary npm dependency resolution; no npm credential file or credential environment value is inspected or printed.
- Verification completeness: focused policy/docs tests, full exact-candidate verification, strict dependency/peer graph, one Zod installation, all twelve runtime/type entries, and repository `typecheck` pass. `git diff --check` for tracked owned paths plus `git diff --no-index --check /dev/null` for the untracked verifier/test paths exited `0`; decisive output was empty before `git status --short` listed only the expected accumulated TASK-01/TASK-02 uncommitted worktree paths.
- Residual risk: the three tarballs are intentionally ignored, non-durable local evidence and must be regenerated/reaccepted by the later controller before any publication. TASK-02 performed no commit, push, publish, Registry mutation, public metadata lookup, or credential access.

## Independent Quick Review Round 1 — reviewer-task02-quick-20260809

- Verdict: `changes_required`.
- Reviewed contract: Task Unit Contract revision 1; referenced Plan T-002, T-003 step 3, BD-002, BD-005, CK-003, CK-004, and CK-006; required TASK-01 dependency output accepted by its Independent Quick Review Round 1.
- Owned diff identity: baseline `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/verify-release-packages.mjs` (`sha256:2acdc1b52866a30f192a893bfc76e6a609a87f95991b27ec1d1a80298a8013d4`), verification/docs portions of `scripts/release-packages.test.mjs` (`sha256:26e2ae366d28363aafbc008e37fd3d586ed03cf6dc10b7e56bfec3af10520c81`), release-script-bearing `package.json` (`sha256:54b47f82ba9af8dfd39af8ec7686db8fc23613bad6be02b5a1b7af3419541daa`), and the bilingual README plus `document/tools/*.md` set (sorted-file-hash aggregate `sha256:1b823f2fd2dd30b849d68e38dcd61fd469cf0f856016111e2fe4dc7053355ed0`). Exact generated evidence inspected: `.release/candidate/manifest.json` (`sha256:adb607a5a6f77391ceab3b145013eb435d20daf1779ad10e4172fc1fd0aa4dd6`).
- Commands/evidence inspected: executor Round 1 RED/GREEN, negative-policy, CK-003/CK-006, CK-004, clean dependency/Zod, twelve-entry runtime/type, repository typecheck, and whitespace records; reviewer reran credential-scrubbed `release:test -- --docs` (3/3 passed), credential-scrubbed `release:verify` (exit 0; candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories 63/103/91; one `zod@3.25.76`; all twelve runtime/type entries), `npm run typecheck` (exit 0), owned whitespace checks (exit 0), and a repository-wide README/document negative scan whose only old aggregate mentions were the intentional no-aggregate statements.
- Finding 1: candidate path containment is not fail-closed. `safeRelative` at `scripts/verify-release-packages.mjs:42-47` proves only lexical containment, while the package root is consumed at `scripts/verify-release-packages.mjs:151-185` without rejecting or canonical-containment-checking the root itself. A reviewer fixture copied the accepted candidate to a unique `os.tmpdir()` directory, replaced `packages/core` with a symlink to the original repository candidate package, and ran policy-only verification; it exited `0` and reported the accepted candidate. This contradicts the Task's safe/isolated candidate and escaping-reference acceptance claim and is not covered by the current negative cases.
- Finding 2: the ordinary verifier invocation does not establish the required credential-free boundary. Both `npm pack` at `scripts/verify-release-packages.mjs:191-194` and clean-consumer `npm install` at `scripts/verify-release-packages.mjs:221-224` inherit the caller environment and npm's default user configuration, so a configured user `.npmrc` or token environment can be read/forwarded despite the Task's explicit `never read local npm credentials` method and the self-check's no-credential-access claim. The review reruns passed only after externally setting `NPM_CONFIG_USERCONFIG=/dev/null` and unsetting common token variables; the implementation itself does not enforce that isolation.
- Required remediation: reject symlinked package roots and archives and prove canonical containment for every candidate/extraction path before reading or extracting; add a negative containment fixture. Invoke all npm subprocesses with an explicit credential-scrubbed environment and empty owned npm user config (without logging values), add focused evidence that credential-bearing caller state cannot reach them, then rerun the required TASK-02 checks and request a fresh independent quick-review round.

## Execution Result Round 2 — executor-task02-20260809

- Remediated Independent Quick Review Round 1 Finding 1 without expanding scope. Candidate manifests, package roots, archive paths, extracted package roots/files, and recursively enumerated candidate/extraction entries now require `lstat`-proven non-symlink type, canonical-root containment, and supported file/directory types before verifier reads. Tar metadata rejects symlink/hard-link entry types before extraction, and extracted inventory is independently re-enumerated under its canonical owned root before byte comparison.
- Added exact containment regressions that replace the copied candidate's core package root and core archive, respectively, with symlinks to the repository candidate. Both now fail closed on symlink/canonical containment rather than consuming the external target.
- Remediated Finding 2 with a per-invocation `mkdtemp` npm-config root containing distinct empty implementation-owned user and global config files. Every verifier-owned `npm pack`, `npm install`, and `npm ls` subprocess receives the same scrubbed environment: caller user/global-config selectors and credential-bearing auth/token/password/username/credential/secret/cert/key variables are omitted by key without reading or logging their values; explicit noncredential settings such as `npm_config_registry`, proxy, and cache remain available for ordinary public dependency fetching. The exact config temporary is removed in `finally`.
- Added a hostile-caller regression that shadows `npm` with a wrapper around the real executable, supplies a token-bearing user config plus `NODE_AUTH_TOKEN`, `NPM_TOKEN`, `GITHUB_TOKEN`, and `npm_config__authToken`, and executes the complete verifier. The wrapper observes at least five npm calls, requires the implementation-owned user config to be empty, rejects any credential-shaped environment key, and proves explicit `npm_config_registry=https://registry.npmjs.org/` still reaches npm; the complete consumer verification exits `0`. No supplied credential value is read or printed by verifier code.
- Round 2 changed paths: `scripts/verify-release-packages.mjs`, the focused containment/credential regression cases in `scripts/release-packages.test.mjs`, and this appended Task evidence only. Earlier execution/review evidence and SDD state were not changed.
- TDD RED: `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `1`; decisive output: `symlinked package root escapes the candidate unexpectedly passed`, while the hostile npm wrapper exited `97` at the first `npm pack`, proving caller credential/config state reached the subprocess. Summary: `tests 4`, `pass 2`, `fail 2`.
- TDD GREEN: after canonical containment and credential-boundary implementation, `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `0`; decisive output: frozen-candidate policy including symlink fixtures, full hostile-environment npm isolation, generator, and docs tests all passed (`tests 4`, `pass 4`, `fail 0`). A first implementation attempt used one file for both npm user/global config and npm correctly rejected double-loading; the two files were separated, preserving the same empty owned-config boundary.
- Final focused/CK-003/CK-006 check: `fnm exec --using=22.22.0 npm run release:test -- --docs` exited `0`; decisive output: `tests 4`, `pass 4`, `fail 0`, including both canonical containment fixtures and the complete hostile npm subprocess probe.
- Final CK-004 check: `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` exited `0` for candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories remain 63/103/91, dependency direction and one `zod@3.25.76` resolution pass, and all twelve runtime plus twelve strict NodeNext entries pass.
- Required type check: `fnm exec --using=22.22.0 npm run typecheck` exited `0`; decisive output: `tsc --noEmit` completed without diagnostics.

## Executor Self-check Round 2 — executor-task02-20260809

- Review remediation: both Round 1 findings are addressed directly. Final package/archive symlinks are rejected; canonical containment is checked before every candidate/extraction read boundary; archive link types are rejected before extraction; all verifier npm subprocess sites use only the owned empty config paths and the shared credential-scrubbed environment.
- Credential discipline: scrub decisions inspect environment key names only. The verifier neither accesses rejected values nor reads the hostile/default user npm config; it creates and uses empty files instead. No credential, npm config content, Registry mutation, publish, or remote operation occurred. Explicit noncredential registry/config propagation is covered by the wrapper regression so ordinary public dependency fetch remains possible.
- Scope and preservation: no docs, source, TASK-01 generator/contract, package metadata, workflow, publication policy, earlier evidence, review verdict, SDD state, Git history, or remote state was changed in Round 2. No commit, push, or publish was attempted.
- Verification completeness: focused tests, full exact-candidate clean consumer, typecheck, and the final owned whitespace checks pass. Residual risk remains the established one: ignored candidate tarballs are local evidence requiring later controller regeneration/reacceptance before publication.

## Independent Quick Review Round 2 — reviewer-task02-quick-r2-20260809

- Verdict: `changes_required`.
- Reviewed contract: Task Unit Contract revision 1; Round 1 findings and Round 2 remediation only; referenced Plan T-002, T-003 step 3, BD-002, BD-005, CK-003, CK-004, and CK-006; required TASK-01 output accepted by its Independent Quick Review Round 1.
- Owned diff identity: baseline `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed Round 2 implementation `scripts/verify-release-packages.mjs` (`sha256:89a2597940575dba91c1310fdbb620274699330914892cb713e7274cf1f2718f`) and focused regressions in `scripts/release-packages.test.mjs` (`sha256:aaf1a4cbae80f902036a7d8abe57d27c55e1bd3cc44899cfb83873f8d51cee4c`). The unchanged release-script-bearing `package.json` is `sha256:54b47f82ba9af8dfd39af8ec7686db8fc23613bad6be02b5a1b7af3419541daa` and exact generated evidence `.release/candidate/manifest.json` is `sha256:adb607a5a6f77391ceab3b145013eb435d20daf1779ad10e4172fc1fd0aa4dd6`.
- Commands/evidence inspected: executor Round 2 RED/GREEN, focused/full/typecheck and self-check records; reviewer reran `fnm exec --using=22.22.0 npm run release:test -- --docs` (4/4 passed), `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` (exit 0; candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories 63/103/91; one `zod@3.25.76`; twelve runtime and twelve strict NodeNext entries), `fnm exec --using=22.22.0 npm run typecheck` (exit 0), and owned whitespace checks (exit 0). A focused BSD-tar probe showed symlink and hard-link entries begin with `l` and `h`, respectively, and are rejected by `archiveInventory` before extraction. A focused npm configuration probe showed that explicit empty user/global configs still load a project `.npmrc` from the subprocess working directory.
- Finding 1: Round 1 credential isolation remains incomplete. `npmEnvironment` at `scripts/verify-release-packages.mjs:148-159` supplies empty user/global configs, but `npm pack` runs with `cwd: candidateRoot` at `scripts/verify-release-packages.mjs:252-256`; npm therefore still reads a candidate-root project `.npmrc`, and `verifyPolicy` never rejects extra root-level files. The reviewer reproduced npm returning a project `.npmrc` registry value while both `NPM_CONFIG_USERCONFIG` and `NPM_CONFIG_GLOBALCONFIG` pointed to empty owned files. An injected candidate-root `.npmrc` can consequently supply auth material to every verifier-owned `npm pack` despite the claimed implementation-enforced credential boundary. The current hostile-caller test creates only a hostile user config and cannot detect this path.
- Finding 2: the credential-key filter and its regression omit one-time passwords. `npmEnvironment` copies `npm_config_otp` because neither filter at `scripts/verify-release-packages.mjs:152-155` matches `otp`; the hostile wrapper's forbidden-key predicate in `scripts/release-packages.test.mjs` likewise omits it. Thus a caller-provided npm OTP credential reaches every npm subprocess while the regression still passes.
- Confirmed remediation: package roots, archives, recursive candidate/extraction entries, and extracted files are now symlink-rejected and canonically contained before their accepted use at `scripts/verify-release-packages.mjs:49-79`, `162-199`, and `226-246`; archive symlink/hard-link entry types are rejected before extraction at `scripts/verify-release-packages.mjs:135-145`. The two root/archive symlink regressions pass.
- Required remediation: prevent every npm subprocess from loading project configuration (for example by using an implementation-owned cwd/config boundary or by rejecting/otherwise neutralizing candidate-root project config before invocation), scrub npm OTP and any equivalent credential-bearing selectors, and extend the hostile regression to prove both paths are absent while explicit noncredential registry behavior remains preserved. Then rerun the focused, full verifier, typecheck, and whitespace checks and request a fresh independent quick-review round.

## Execution Result Round 3 — executor-task02-20260809

- Remediated Independent Quick Review Round 2 Finding 1 by moving every verifier-owned `npm pack <absolute-package-root> --json --dry-run` subprocess from the candidate root to the canonical implementation-owned npm config directory. Absolute package targets preserve pack semantics and inventory results, while candidate/project `.npmrc` discovery through `cwd` is eliminated. Consumer `npm install`/`npm ls` continue in their newly created verifier-owned consumer directory, which contains only the implementation-created package/consumer files.
- Added a candidate-project-config regression that copies the exact candidate outside the repository, injects a candidate-root `.npmrc` containing a hostile registry marker and auth line, and shadows npm with a wrapper around the real executable. Before each pack, the wrapper invokes real `npm config get registry` under the received cwd/env and fails with exit `96` if the hostile candidate registry is visible. The accepted result records exactly three pack calls with no candidate project config reach.
- Remediated Finding 2 by adding `otp` to both generic credential-shaped environment-key filtering and the stricter `npm_config_*` credential selector filtering. The hostile caller regression now injects `npm_config_otp` together with token/auth variables; its wrapper rejects any surviving OTP key. The test also proves noncredential `npm_config_fetch_retries=7` remains available, preserving ordinary explicit npm behavior without using credential state.
- Round 3 changed paths: `scripts/verify-release-packages.mjs`, the focused OTP/project-config regression portions of `scripts/release-packages.test.mjs`, and this appended Task evidence only. Prior evidence, reviews, SDD state, documentation, package metadata, generator, candidate content, and publication surfaces were not edited.
- TDD RED: `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `1`. The complete hostile caller test failed at the first pack with wrapper exit `97` because `npm_config_otp` reached npm; the independent candidate-root config test failed at the first pack with wrapper exit `96` because real npm resolved `https://candidate-config.invalid/` from the injected project `.npmrc`. Decisive summary: `tests 5`, `pass 3`, `fail 2`.
- TDD GREEN: `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `0` after the minimum cwd and OTP-key fixes; decisive output: candidate policy, hostile credential/OTP boundary, hostile candidate project-config boundary, generator, and docs all passed (`tests 5`, `pass 5`, `fail 0`). A routine first rerun exposed a locally misplaced canonical npm-root declaration (`ReferenceError`); moving it into the existing main config-root scope corrected the implementation without changing behavior or scope.
- Final focused/CK-003/CK-006 check: `fnm exec --using=22.22.0 npm run release:test -- --docs` exited `0`; decisive output: `tests 5`, `pass 5`, `fail 0`, including complete hostile caller verification and three project-config-isolated pack invocations.
- Final CK-004 check: `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` exited `0` for unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories remain 63/103/91, dependency direction and one `zod@3.25.76` resolution pass, and all twelve runtime plus twelve strict NodeNext entries pass.
- Required type check: `fnm exec --using=22.22.0 npm run typecheck` exited `0`; decisive output: `tsc --noEmit` completed without diagnostics.

## Executor Self-check Round 3 — executor-task02-20260809

- Review remediation: npm pack no longer has a candidate/project-config discovery cwd; the exact absolute package roots remain pack inputs. `npm_config_otp` and equivalent key shapes are removed without reading their values. Both review findings have independent failing-before/passing-after probes using the real npm executable beneath a non-secret test wrapper.
- Credential discipline: verifier code still makes decisions from environment key names only, never reads rejected values or candidate/default npm credential config, and never logs credential names or values. Empty owned user/global files remain mandatory. Explicit noncredential npm options remain propagatable and tested.
- Scope and preservation: Round 3 changes only the two requested credential-isolation behaviors and their tests/evidence. No source, docs, package contract, candidate, earlier evidence/review, workflow/controller state, Git history, remote, Registry, token, commit, push, or publish operation was changed or accessed.
- Verification completeness: focused policy/docs tests, full frozen-candidate consumer verification, strict dependency/type/runtime checks, typecheck, and final owned whitespace checks pass. The result is ready for a fresh independent quick review.

## Independent Quick Review Round 3 — reviewer-task02-quick-r3-20260809

- Verdict: `passed`.
- Reviewed contract: Task Unit Contract revision 1; Independent Quick Review Round 2 findings and Execution/Self-check Round 3 remediation only. This shallow re-review covered candidate/project `.npmrc` isolation for all verifier-owned npm subprocesses, OTP/key-name credential scrubbing, preservation of the accepted symlink/canonical safeguards, and material clean-consumer regressions.
- Owned diff identity: baseline and current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/verify-release-packages.mjs` (`sha256:6d2da36f4b9a52198c62667b021469e0aba1ba85074c998d0b91971dd54de731`) and focused regressions in `scripts/release-packages.test.mjs` (`sha256:f54fcca61e047d08526ad2aefbf6a576348ef62bfbc2c54f459462d7cf6efff8`). Unchanged supporting identities were `package.json` (`sha256:54b47f82ba9af8dfd39af8ec7686db8fc23613bad6be02b5a1b7af3419541daa`) and `.release/candidate/manifest.json` (`sha256:adb607a5a6f77391ceab3b145013eb435d20daf1779ad10e4172fc1fd0aa4dd6`).
- Commands/evidence inspected: Round 2 findings, Round 3 RED/GREEN and self-check evidence, the current npm environment/cwd call sites and focused hostile wrappers; reviewer freshly ran `fnm exec --using=22.22.0 npm run release:test -- --docs` (5/5 passed), `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` (exit 0; unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories 63/103/91; one `zod@3.25.76`; all twelve runtime and strict NodeNext type entries passed), `fnm exec --using=22.22.0 npm run typecheck` (exit 0), and tracked/untracked owned whitespace checks (no diagnostics).
- Finding disposition: Round 2 Finding 1 is resolved. Each `npm pack` uses an absolute, canonically contained package target while running from the implementation-owned canonical npm-config directory; `npm install` and `npm ls` run only in the newly created implementation-owned consumer root. The hostile candidate-root `.npmrc` regression observes all three pack calls and proves the candidate project registry/auth config is not discovered.
- Finding disposition: Round 2 Finding 2 is resolved. `npmEnvironment` removes OTP along with credential-shaped caller environment keys based only on key names, overrides user/global npm config selectors with distinct empty owned files, and the hostile full-verifier wrapper proves `npm_config_otp` and other credential selectors do not reach any of at least five npm calls while an explicit noncredential option remains available.
- Regression disposition: the prior `lstat` symlink rejection, canonical containment, archive link rejection, extracted-tree verification, clean non-symlink consumer, strict dependency graph, single Zod resolution, and complete runtime/type entry probes remain present and passed. No obvious blocking defect or material regression was demonstrated in the bounded Round 3 inputs.
