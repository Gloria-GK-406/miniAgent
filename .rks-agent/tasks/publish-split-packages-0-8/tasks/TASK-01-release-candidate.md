# TASK-01 — Generate the split release candidate

## Task Unit Contract

- Contract revision: 1
- Goal and deliverables: Implement the canonical three-package 0.8.0 contract and a deterministic generator that projects the existing build into frozen core, engine, and extensions package candidates without changing source-layer imports; set the repository root to the private 0.8.0 source release.
- Referenced spec requirements: RQ-001, RQ-002, RQ-003, RQ-004, RQ-005, RQ-006, RQ-007, RQ-008, RQ-016, RQ-017.
- Referenced plan sections: T-001, T-003 steps 1-2; BD-001, BD-003; CK-001, CK-002, CK-005.
- Owned boundary: Canonical release contract, generated manifests/exports/dependency ranges, package projection and generated core-reference rewriting, common candidate identity, archive inventory/SRI creation, deterministic build, and private root release metadata.
- Permitted files or interfaces: `scripts/release/package-contract.mjs`, `scripts/build-release-packages.mjs`, generation-focused cases in `scripts/release-packages.test.mjs`, `package.json`, `package-lock.json`, and a narrow generated-output ignore entry when needed. Generated `.release/` content is evidence only and must not be committed.
- Dependencies: None.
- Required dependency outputs: Existing successful TypeScript `dist/core`, `dist/engine`, and `dist/extensions` build output at accepted baseline `838cb8c`.
- Acceptance conditions: Exactly three public 0.8.0 package candidates are generated; each export target exists; core has no MiniAgent dependency; engine/extensions normally depend on core 0.8.0 and not each other; external ranges and Zod peer satisfaction match the approved Spec; no CLI/tests/wrong layer or escaping core reference is packaged; every package exposes the same Registry-visible joint candidate identity; root is private 0.8.0; repeat generation under the pinned pack contract is byte-identical.
- Task-specific authorization: Root TaskContract authorization for local implementation and validation only; no Git commit, push, npm credential access, or Registry mutation.
- Required methods: Apply test-driven-development for executable generator behavior. Record exact RED and GREEN commands/output. Keep source imports unchanged and rewrite only generated runtime/declaration artifacts. Use immutable/minimally scoped filesystem operations and reject unsafe output paths.
- Required checks: Focused generator tests; `npm run build`; generated manifest/archive inspection; deterministic double generation; `git diff --check` for owned files.
- Required evidence: Appended execution result with changed paths, RED/GREEN evidence, generated three-package inventory and identities, root metadata proof, focused check exits, executor self-check, and a matching passed independent quick-review round.

## Contract Amendment A1 — Root publication regression expectation

- Reason: The full suite exposed `src/cli/package.test.ts` still asserting the removed aggregate `prepublishOnly` hook, directly contradicting this Task's approved private-root acceptance rather than representing unrelated behavior.
- Added permitted file: `src/cli/package.test.ts`, limited to replacing the obsolete aggregate-publish expectation with assertions that the root is private and has no aggregate publish hook/configuration. Preserve local CLI build and smoke expectations.
- Added check: Re-run the focused package metadata test and the complete `npm test` suite after the correction.

## Execution Result Round 1 — executor-task01-20260809

- Implemented the canonical `0.8.0` three-package contract and pinned packing contract (`Node 22.22.0`, `npm 10.9.4`), a safe/idempotent generator, generated-only core-reference and source-map normalization, common candidate identity, archive inventories and SHA-512 SRI identities, deterministic double generation, and private root release metadata. Unified `src/` imports were not changed.
- Changed paths: `scripts/release/package-contract.mjs`, `scripts/build-release-packages.mjs`, `scripts/release-packages.test.mjs`, `package.json`, `package-lock.json`, `src/cli/package.test.ts`, and this Task evidence file. Generated `.release/candidate/` remains ignored evidence only.
- TDD RED: `node --test scripts/release-packages.test.mjs` exited `1`; decisive output: `Cannot find module .../scripts/build-release-packages.mjs`, proving the generator behavior was absent rather than the test being malformed.
- TDD GREEN: `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` exited `0`; decisive output: `tests 1`, `pass 1`, `fail 0` after the minimal generator implementation and generated core-reference correction.
- Pinned build/determinism check: `fnm exec --using=22.22.0 zsh -c 'npm run release:toolchain && npm run build && npm run release:build -- --verify-determinism'` exited `0`; decisive output: `release toolchain ok: node 22.22.0, npm 10.9.4`, successful `tsc`, and `generated 3 packages for sha256:e3f9051572c9305864a1764b8bae0349092b19fcc15a476f3d79444f0bceb409` after byte-identical isolated generations.
- CK-002 identity check: `fnm exec --using=22.22.0 node -e "const c=require('./.release/candidate/manifest.json'); const ids=new Set(c.packages.map(p=>p.candidateId)); if(c.packages.length!==3||new Set(c.packages.map(p=>p.name)).size!==3||ids.size!==1||!ids.has(c.candidateId)) process.exit(1)"` exited `0`.
- CK-005 root/lock check: `fnm exec --using=22.22.0 zsh -c 'npm install --package-lock-only && npm ci && node -e "const p=require(\"./package.json\"); if(p.version!==\"0.8.0\"||p.private!==true||p.publishConfig||p.scripts?.prepublishOnly) process.exit(1)"'` exited `0`; decisive metadata: `0.8.0`, `private: true`, `npm@10.9.4`, Node `22.22.0`. `npm ci` reported 17 dependency-audit findings (2 low, 5 moderate, 10 high) without failing the required lock check; no audit remediation was in scope.
- Generated manifest/archive inspection command recalculated every archive SRI, compared every generated dependency object with the canonical contract, opened every export target, and scanned candidate text/layers; it exited `0` with `candidate reference/layer scan: clean`.
- Candidate source revision: `838cb8cb9b86046df1d3dac94702a48438c28692`; common candidate ID: `sha256:e3f9051572c9305864a1764b8bae0349092b19fcc15a476f3d79444f0bceb409`.
- Candidate inventory and exact identities: `@piaoxianguo/miniagent-core` — 1 export, 63 files, `sha512-hqgo3S951bavspuWhRIOKph9iCEwPEvuQN6tUG2IbxddKHWD3xCE9McKwy7s78+LgvGYl+isZ/LoXpDc24km0w==`; `@piaoxianguo/miniagent-engine` — 7 exports, 103 files, `sha512-GT3ofThUMnVYDJK+J/fMNc9W+/ziVH5BBApHSbJbhZi3gAbj+6YmXhL4W9OKuAxtFMvXUJNT3/HfcBhy6lQ0Jw==`; `@piaoxianguo/miniagent-extensions` — 4 exports, 91 files, `sha512-BOxAgyXbBa101TyOivEXnQI7tIWjHLyRLSndqjjeLw+Yp7sA9+McCwV3r0/UXfJy7jTvB8ZvbxUCUivkUYWySw==`.
- A1 RED: `fnm exec --using=22.22.0 zsh -c 'npm run lint && npm run release:test && npm test'` exited `1` only because `src/cli/package.test.ts:57` expected the now-forbidden aggregate `prepublishOnly`; decisive suite output was `1 failed | 138 passed`, `1 failed | 1094 passed`.
- A1 GREEN/regression: `fnm exec --using=22.22.0 zsh -c 'npx vitest run src/cli/package.test.ts && npm run release:test && npm test'` exited `0`; decisive output: CLI metadata `7 passed`, generator `pass 1 / fail 0`, full suite `139 passed` and `1095 passed`.
- Lint: the lint portion of the A1 RED command completed successfully before the expected legacy-test failure; decisive output: `dependency architecture valid: src`.

## Executor Self-check Round 1 — executor-task01-20260809

- Scope: all implementation edits are confined to the original TASK-01 permitted paths plus the single A1-authorized CLI metadata expectation; no source-layer imports, CLI behavior, credentials, Registry state, Git history, remote, commit, push, or publish operation was changed.
- Contract: exactly the approved core/engine/extensions names, exports, external ranges, internal core `0.8.0` dependencies, and no horizontal/internal dependency from core are synthesized from one canonical file. Every archive target exists and generated maps/references are package-local or use the public core root.
- Determinism/safety: only the exact `.release/candidate` or a generator-owned temporary `candidate` directory can be removed; symlinks and unexpected compiled file classes are rejected; double-generation snapshots include manifests, normalized trees, candidate IDs, and tarball bytes.
- Root metadata: root is `0.8.0`, private, lock-synchronized, retains local CLI `bin`, build and smoke scripts, and has neither `publishConfig` nor `prepublishOnly`.
- Verification: focused generator, focused root/CLI metadata, lint, build, deterministic generation, manifest/SRI/export/layer inspection, lock reproduction, and the complete unit suite have successful evidence above. Final whitespace validation is recorded after this evidence append.
- Final whitespace validation: `git diff --check -- package.json package-lock.json src/cli/package.test.ts` plus `git diff --no-index --check /dev/null <file>` for each untracked/evidence-owned path completed with wrapper exit `0`; decisive output: `git diff whitespace checks: clean (tracked and untracked owned files)`.
- Residual risks: `npm ci` reports 17 existing dependency audit findings; they do not affect the required checks and were not modified. Generated `.release/` is intentionally non-durable evidence and must remain uncommitted.

## Independent Quick Review Round 1 — reviewer-task01-quick-20260809

- Verdict: `passed`.
- Reviewed contract: Task Unit Contract revision 1 plus Amendment A1; referenced Plan T-001, T-003 steps 1-2, BD-001, BD-003, CK-001, CK-002, and CK-005, with the Spec External Behavior and Data Rules used only to confirm the frozen package/export/dependency table.
- Owned diff identity: baseline `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `package.json` (`sha256:45452cc852a05d47104801be64f21c79179caf049ea2919ab88167d3137d89b6`), `package-lock.json` (`sha256:3b0c0d8517ddd09cefd377f3b1096809bf114bf9a2aec91acdb760fbc9b11643`), `src/cli/package.test.ts` (`sha256:676ce05300b7a28cb14f60a8233b8aae11b3865e6f686d958941df72673f2909`), `scripts/build-release-packages.mjs` (`sha256:3a39d0682f8b4cbbf86a4cfe15600d14b2d8771c4150501889e2aa4993b34e9d`), `scripts/release/package-contract.mjs` (`sha256:318a04aa9ac219b2609f1777678b59839d4974395c74b5d1d2cfb4ecb2639f09`), and the generation-focused `scripts/release-packages.test.mjs` (`sha256:edafdb22ed7dae0645b4560961fc24816d2339bc21e1281691fc57614e99b915`). Generated `.release/candidate/` was inspected as non-durable evidence, not as owned source.
- Commands/evidence inspected: executor Round 1 RED/GREEN, pinned build/determinism, CK-002, CK-005, generated manifest/archive inspection, A1 regression/full-suite, lint, and whitespace records; reviewer reran `fnm exec --using=22.22.0 node --test scripts/release-packages.test.mjs` (1/1 passed), `fnm exec --using=22.22.0 zsh -c 'npm run release:toolchain && npm run build && npm run release:build -- --verify-determinism'` (exit 0; exact Node/npm accepted; three packages regenerated with candidate `sha256:e3f9051572c9305864a1764b8bae0349092b19fcc15a476f3d79444f0bceb409`), an independent archive-SRI/export-target/inventory check (exit 0; inventories 63/103/91), `fnm exec --using=22.22.0 npx vitest run src/cli/package.test.ts` (7/7 passed), generated relative-core/layer scan (no forbidden match), and owned-file whitespace checks (exit 0).
- Findings: no obvious blocking defect was demonstrated. The canonical contract at `scripts/release/package-contract.mjs:9-71` matches the approved three names, exports, dependency direction, bounded ranges, and exact packing toolchain; the generator at `scripts/build-release-packages.mjs:48-69`, `92-118`, `135-170`, and `172-264` constrains removal targets, rejects symlinks/unexpected artifact classes, rewrites generated core references, validates exports/content, binds a common identity, records archive SRI, and proves byte-identical isolated generation. Root metadata at `package.json:1-21` and `package.json:106-153` is private 0.8.0 with synchronized ranges and no aggregate publication hook/configuration, and Amendment A1 is covered at `src/cli/package.test.ts:53-60`. The controller-recovered `node_modules` symlink was outside the Task-owned product diff and did not alter this verdict.
- Required remediation: none.

## Rework Execution Round 2 — DRIFT-01 — executor-task01-rework-20260809

- Remediated only DRIFT-01 in `scripts/build-release-packages.mjs`. Every generator-owned npm subprocess—one `npm --version` toolchain query and each `npm pack` invocation—now uses one generator-owned `mkdtemp` config boundary with distinct empty user/global npm config files, a canonical temporary cwd that cannot discover repository/candidate project `.npmrc`, and a key-name-only credential-scrubbed environment. Auth/token/OTP/password/username/credential/secret/cert/key selectors are omitted without reading values; explicit noncredential registry/proxy/cache/options remain available and `SOURCE_DATE_EPOCH=0` remains fixed for exact pack semantics. The exact owned config root is removed in `finally`.
- Added one generator-focused hostile regression in `scripts/release-packages.test.mjs`. It creates an exact temporary repository `.npmrc` containing a fake hostile registry/auth marker, a fake token-bearing caller user config, and fake caller `NODE_AUTH_TOKEN`, `NPM_TOKEN`, `npm_config__authToken`, and `npm_config_otp`; it shadows npm with a wrapper over the real pinned npm. The wrapper rejects any credential-shaped key, nonempty selected user config, visible hostile project registry, or loss of explicit noncredential `npm_config_fetch_retries=7`, and records exactly one `--version` plus three `pack` calls. Fake values are test literals only and are never read/logged by generator code; the exact project config is removed in `finally`.
- Rework changed paths: `scripts/build-release-packages.mjs`, the generator-focused regression in `scripts/release-packages.test.mjs`, and this appended TASK-01 evidence only. The canonical contract, root metadata/lock, CLI expectation, candidate contents, prior evidence/reviews, drift review, workflow/controller state, and TASK-02/TASK-03 implementation were not edited.
- TDD RED: `fnm exec --using=22.22.0 node --test --test-name-pattern='isolates every generator' scripts/release-packages.test.mjs` exited `1`; decisive output: the first `npm --version` subprocess reached the hostile wrapper and exited `95`, with summary `tests 1`, `pass 0`, `fail 1`. This demonstrated the missing generator credential/project-config boundary rather than a malformed test.
- TDD GREEN: the same focused command exited `0` after the minimum npm context/environment implementation; decisive output: `tests 1`, `pass 1`, `fail 0`, and the wrapper log asserted one toolchain query plus all three pack operations.
- Focused TASK-01 generator check: `fnm exec --using=22.22.0 node --test --test-name-pattern='generates one deterministic|isolates every generator' scripts/release-packages.test.mjs` exited `0`; decisive output: deterministic candidate generation and hostile npm isolation both passed (`tests 2`, `pass 2`, `fail 0`).
- Pinned build/determinism check: `fnm exec --using=22.22.0 zsh -c 'npm run release:toolchain && npm run build && npm run release:build -- --verify-determinism'` exited `0`; decisive output: `release toolchain ok: node 22.22.0, npm 10.9.4`, successful `tsc`, and three packages generated deterministically for unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`. The CK-002 exact three-name/common-ID one-liner then exited `0`.
- Root/lock and repository regression check: `fnm exec --using=22.22.0 zsh -c 'npm install --package-lock-only && npm ci && <private-0.8/no-publish metadata assertion> && npm run lint && npm test'` exited `0`; root remains private `0.8.0` without `publishConfig`/`prepublishOnly`, lint reports `dependency architecture valid: src`, and Vitest reports `139 passed` files / `1095 passed` tests. npm retained the existing 17 audit findings (2 low, 5 moderate, 10 high) without scope expansion.
- Integrated release check: `fnm exec --using=22.22.0 zsh -c 'npm run release:test && npm run release:verify -- --candidate .release/candidate/manifest.json'` exited `0`; decisive output: complete release suite `tests 10`, `pass 10`, `fail 0`, inventories 63/103/91, normal engine/extensions-to-core graph, one `zod@3.25.76`, and all twelve runtime plus twelve strict NodeNext entries passed.
- Integrated credential-free dry-run: `env -u NODE_AUTH_TOKEN fnm exec --using=22.22.0 npm run release:publish -- --candidate .release/candidate/manifest.json --dry-run --registry-fixture scripts/fixtures/registry-none.json` exited `0` and printed only the three retained core-engine-extensions `.tgz` provenance commands. No credential or Registry mutation was used.
- Candidate identities after rework: core — 63 files, `sha512-cMlDl77Mlwm+wS1n09P9sbfIMfztaP+DdhDgaQLH02464XtE1BL9RmMon1OzwHspB/V6hnSa4h1qtSj2e0NlFQ==`; engine — 103 files, `sha512-8XWvWQesLxMBQenbrgYZKqmjzNgEtQZW03YYpVnF3kDqRXGl/dqy97alr1L0XgrZdlTWTR2SbIw58wPGSUXIzw==`; extensions — 91 files, `sha512-jfxhjOUEbgHeSwvpJ4VXLs9e+1FGubr5vIMY/VPqhmaW2N8qu8rVwH2AVQzrn+c3FpbQC0Sc0f+37I4juSAxHA==`. The joint candidate ID and package payload identities are unchanged by npm config isolation.

## Rework Executor Self-check Round 2 — DRIFT-01 — executor-task01-rework-20260809

- DRIFT-01 closure: all generator-owned npm call sites share the implementation-owned empty user/global config, canonical non-project cwd, credential-key scrubber including OTP, and deterministic environment. There is no remaining generator npm call using repository cwd or unfiltered caller environment.
- Test fidelity: the wrapper delegates to the real pinned npm, probes npm's effective registry from the received cwd/config, and covers the toolchain lookup plus every package pack. It proves project and caller credential paths are absent while a benign npm option is preserved; it does not inspect real credentials.
- Safety/scope: generated package trees, archive format, deterministic identity algorithm, source import rewriting, path/removal constraints, package contract, and public surface are unchanged. Only exact generator/test/evidence paths were edited. The temporary `.npmrc` and generator npm config roots were removed; `test ! -e .npmrc` passed.
- Verification/residue: focused generator, pinned toolchain, build/determinism, exact identity, root/lock, lint, complete repository suite, complete release/consumer suite, credential-free publication dry-run, and owned whitespace checks pass. Generated `.release` remains ignored evidence; no file is staged and no credential/token/config residue, commit, push, publish, Registry access/mutation, prior evidence/review alteration, or drift-state mutation occurred.

## Independent Quick Review Round 2 — reviewer-task01-quick-r2-20260809

- Verdict: `changes_required`.
- Reviewed contract: Task Unit Contract revision 1 plus Amendment A1; DRIFT-01 and Rework Round 2 only; approved Plan T-001, T-003 steps 1-2, BD-001, BD-003, CK-001, CK-002, and CK-005.
- Owned diff identity: baseline `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/build-release-packages.mjs` (`sha256:0ff515b9e2431b69da15074f6d1455395e7ec84e24edc651106337f200abb54a`) and the generator-focused regression in `scripts/release-packages.test.mjs` (`sha256:9b4812e9550bdda31d9e16e68e70a7d25151bf833c44628d83ca1938049ad884`). Exact generated evidence `.release/candidate/manifest.json` is `sha256:adb607a5a6f77391ceab3b145013eb435d20daf1779ad10e4172fc1fd0aa4dd6`.
- Commands/evidence inspected: DRIFT-01 finding, Rework Round 2 RED/GREEN, focused generator, pinned build/determinism, CK-002, integrated release, root/lock/full-suite, publication dry-run, self-check, and whitespace records. Reviewer reran the focused deterministic/hostile generator cases (2/2 passed), exact toolchain plus build and deterministic double generation (exit 0; unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories 63/103/91), CK-002 identity (exit 0), no-residual-root-`.npmrc` check (exit 0), and owned whitespace checks (exit 0).
- Confirmed remediation: all four generator-owned npm calls use the shared implementation-owned context. `npmEnvironment` at `scripts/build-release-packages.mjs:44-56` filters credential-shaped variables including OTP by key before reading retained values, replaces user/global config with empty owned files, preserves benign npm options, and fixes `SOURCE_DATE_EPOCH=0`; `createNpmContext` and the call sites at `scripts/build-release-packages.mjs:59-77`, `233-237`, and `273-300` use a canonical repository/candidate-external cwd and remove only the exact owned temporary. The wrapper regression delegates one `--version` and all three `pack` calls to pinned npm, checks effective project registry isolation, empty selected user config, OTP/auth/token absence, and preservation of `npm_config_fetch_retries=7`. Fresh deterministic generation confirms pack payload and identity semantics are unchanged.
- Finding: the hostile project-config regression is not safely contained. At `scripts/release-packages.test.mjs:107-115` it targets the real worktree-root `.npmrc` and calls `writeFile` without first proving the path absent or using exclusive creation; at `scripts/release-packages.test.mjs:149-151` it then unconditionally removes that path. If a caller already has a repository `.npmrc`—including one containing local credentials—the test overwrites and deletes it. This is an obvious unsafe operation inside the exact credential-boundary remediation and conflicts with TASK-01's no-local-credential-access/minimally scoped filesystem method. The current passing run only proves this worktree happened not to contain `.npmrc`.
- Required remediation: make creation of the temporary hostile repository config fail closed and non-destructive (at minimum, atomically require the target to be absent and remove it only when this test created it; a fully contained copied-repository probe is also acceptable), while continuing not to read or log any pre-existing credential/config value. Rerun the focused hostile generator case, deterministic build/CK-002, no-residue assertion, and owned whitespace checks, then request a fresh independent quick-review round.

## Rework Execution Round 3 — TASK-01 quick-review remediation — executor-task01-rework-r3-20260809

- Remediated only Independent Quick Review Round 2's test-containment finding in `scripts/release-packages.test.mjs`. The hostile generator regression now creates a fully owned `mkdtemp/repository` fixture and copies only `scripts/build-release-packages.mjs`, `scripts/release/package-contract.mjs`, `dist`, `LICENSE`, and `README.md`. Its hostile project `.npmrc`, generated `.release/candidate`, wrapper, user config, and logs all live under that one temporary root and are removed together.
- The isolated fixture uses the current worktree Git directory read-only through `GIT_DIR` plus the temporary fixture as `GIT_WORK_TREE`, allowing the copied generator to resolve the same source revision without initializing or committing any temporary or real Git history. No real worktree-root `.npmrc` path is opened, read, written, renamed, or removed by the regression.
- The original hostile oracle is preserved: a wrapper delegates to the real pinned npm, rejects credential-shaped caller variables including OTP, requires an empty implementation-owned user config, probes effective registry to reject discovery of the fixture project `.npmrc`, preserves `npm_config_fetch_retries=7`, and records exactly one `npm --version` plus three `npm pack` operations. The generated fixture manifest is asserted present.
- Round 3 changed paths: only the generator-focused test block in `scripts/release-packages.test.mjs` and this appended TASK-01 evidence. `scripts/build-release-packages.mjs` production implementation, canonical contract, candidate contents, prior evidence/reviews, drift review, SDD state, and all TASK-02/TASK-03 paths were not edited.
- TDD disposition: no new production behavior was introduced, so a new RED implementation cycle was not applicable. The behavior-level TDD RED from Rework Round 2 remains authoritative; this round is a safety containment refactor of that already-passing oracle. The first post-refactor focused execution is the faithful replacement GREEN evidence below.
- Focused hostile fixture check: `fnm exec --using=22.22.0 node --test --test-name-pattern='isolates every generator' scripts/release-packages.test.mjs` exited `0`; decisive output: `tests 1`, `pass 1`, `fail 0`. All hostile project/caller isolation assertions and the exact four npm-call inventory passed from the contained copy.
- Proportionate TASK-01 check: `fnm exec --using=22.22.0 node --test --test-name-pattern='generates one deterministic|isolates every generator' scripts/release-packages.test.mjs` exited `0` (`tests 2`, `pass 2`, `fail 0`). `fnm exec --using=22.22.0 zsh -c 'npm run release:toolchain && npm run build && npm run release:build -- --verify-determinism'` exited `0` with exact Node 22.22.0/npm 10.9.4, successful `tsc`, and unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; CK-002 exact three-name/common-ID check exited `0`.
- Integrated regression: `fnm exec --using=22.22.0 zsh -c 'npm run release:test && npm run release:verify -- --candidate .release/candidate/manifest.json'` exited `0`; decisive output: complete release suite `tests 10`, `pass 10`, `fail 0`, inventories 63/103/91, one `zod@3.25.76`, and all twelve runtime plus twelve strict NodeNext entries passed.

## Rework Executor Self-check Round 3 — TASK-01 quick-review remediation — executor-task01-rework-r3-20260809

- Containment: every path mutated by the hostile regression is a descendant of its unique `mkdtemp` root. Cleanup removes only that exact root. The real repository is used only as a read source for fixture copies and its Git directory for `rev-parse HEAD`; no repository config content is read.
- Preservation: production generator hash/content and DRIFT-01 behavior remain unchanged. Candidate identity, pack semantics, credential-key scrubbing, empty user/global npm configs, canonical npm cwd, deterministic generation, source revision, and public package surface are unaffected.
- Scope/safety: no implementation outside the permitted generation-focused test was edited. No real `.npmrc`, credential value, token, Git history, prior evidence/review, drift state, remote, Registry, commit, push, or publish operation was accessed or mutated.
- Verification/residue: focused hostile, focused deterministic generator, pinned toolchain/build/determinism, CK-002, complete release/consumer verification, no-root-`.npmrc`, and owned whitespace checks pass. The result is ready for a fresh independent TASK-01 quick review.

## Independent Quick Review Round 3 — reviewer-task01-quick-r3-20260809

- Verdict: `passed`.
- Reviewed contract: Task Unit Contract revision 1 plus Amendment A1; DRIFT-01, Independent Quick Review Round 2's containment finding, and Rework Execution/Self-check Round 3 only. This shallow review covered hostile-regression filesystem containment, preservation of repository/project config and credential/OTP isolation, and material generator/determinism regressions.
- Owned diff identity: baseline and current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; unchanged production generator `scripts/build-release-packages.mjs` (`sha256:0ff515b9e2431b69da15074f6d1455395e7ec84e24edc651106337f200abb54a`) and Round 3 generation-focused `scripts/release-packages.test.mjs` (`sha256:9429013d065023c413482bd149a04c86aa321feba60e66cdbe14ea25a45e8524`). Supporting canonical contract remained `sha256:318a04aa9ac219b2609f1777678b59839d4974395c74b5d1d2cfb4ecb2639f09`; generated manifest evidence remained `sha256:adb607a5a6f77391ceab3b145013eb435d20daf1779ad10e4172fc1fd0aa4dd6`.
- Commands/evidence inspected: complete TASK-01, DRIFT-01, Round 2 finding, Round 3 execution/self-check, current generator and contained test block; reviewer freshly ran the focused deterministic/hostile generator cases (2/2 passed), exact toolchain plus build and deterministic double generation (exit 0; Node 22.22.0/npm 10.9.4; unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`), CK-002 exact three-name/common-ID assertion (exit 0), real-worktree `.npmrc` absence check (exit 0), and tracked/untracked owned whitespace checks (no diagnostics).
- Finding disposition: Round 2's unsafe test operation is resolved. The regression creates one unique `mkdtemp` root and writes its copied generator/contract/build inputs, hostile project `.npmrc`, hostile user config, wrapper, log, and generated candidate only beneath that root at `scripts/release-packages.test.mjs:105-160`; its sole recursive cleanup target is that exact owned root. The real worktree is only the source of explicit fixture copies, and its Git directory is supplied read-only to the fixed generator `git rev-parse HEAD` call. No code path in the regression opens, writes, renames, or removes the real worktree `.npmrc` or any user file.
- Regression disposition: the contained wrapper still exercises and delegates exactly one `npm --version` and three `npm pack` calls, rejects credential-shaped variables including `npm_config_otp`, requires the implementation-owned empty user config, proves the fixture repository `.npmrc` registry is not discovered, preserves `npm_config_fetch_retries=7`, and asserts candidate generation completes. The production generator hash matches Round 2, and fresh deterministic generation retained the same joint candidate ID. No obvious blocking defect or material regression was demonstrated.

## Rework Execution Result Round 4 — TASK-01 provenance repository projection — executor-task01-rework-r4-20260810

- Bounded correction: remediated only Change Review Round 5 BF-1 in the TASK-01 canonical contract, generator, verifier, generation/policy regressions, and this appended evidence. Workflow/BF-2, publication/Registry behavior, package source layers, prior evidence/reviews/challenges/drift, and SDD state were not changed.
- Canonical repository authority: `scripts/release/package-contract.mjs` now owns one frozen exact repository object `{type:"git",url:"git+https://github.com/Gloria-GK-406/miniAgent.git"}`. The generator reads root `package.json` and fails unless its repository object exactly equals that contract, then projects the contract object into every public manifest. The verifier independently requires the root object and builds every expected manifest from the same contract; production literals are not duplicated.
- Identity/packing order: repository metadata is added by `packageManifest` before construction of marker-empty manifests and joint tree hashing, and remains in the final manifests before `npm pack`. It therefore participates in both the common SHA-256 candidate identity and every retained archive SHA-512 SRI rather than being patched after acceptance.
- Directory/archive enforcement: verifier exact-manifest equality rejects missing repository, wrong type, wrong owner/URL, and case changes in generated directories. After safe extraction it parses the retained tarball's own `package/package.json` and requires the same complete expected manifest before byte-by-byte tree comparison. Positive generation assertions read all three directories and all three tarball-embedded manifests and require the exact case-preserving object, providing the npm provenance-readiness check.
- Root/negative regressions: the contained generator fixture now copies root metadata, succeeds with the canonical object, then changes only repository casing and proves generation fails with a root-repository diagnostic. Candidate-policy cases independently reject absent repository, wrong type, foreign URL, wrong casing, and a repacked archive whose manifest repository is wrong while its candidate record SRI is updated so execution reaches extracted-manifest validation.
- TDD RED: pinned focused deterministic/policy command exited `1` (`0/2`, `4.26s`). The generated-directory assertion observed `repository === undefined`, and the missing-repository candidate unexpectedly passed, directly reproducing both generator and verifier gaps. The first GREEN attempt exposed a fixture-only BSD tar root-entry difference; repacking the same top-level archive entries without the extra root entry preserved npm inventory shape and reached the intended archive-manifest assertion.
- TDD GREEN/current checks: generator/verifier syntax and final focused deterministic/hostile-root/policy command exited `0` (`3/3`, `6.20s`); archive policy alone exited `0` (`1/1`, `3.08s`). Final pinned release suite exited `0` (`14/14`, `30.43s`). Deterministic double generation and exact release verification exited `0` with new joint candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, unchanged inventories `63/103/91`, one `zod@3.25.76`, required graph, and 12 runtime/12 type entries.
- New retained identities: core `sha512-db/z9+jPKuQVrVZBZ4ayIi3K4j9nQWhUDMDvpQ87TMYTSuAr7Vqm58k1hiORDSR6M8ymMXthrzYmuvdWirCqLA==`; engine `sha512-In5ZLPmvyQfzpxzkbSkBReGa7yoOSjYUzkqcTB7lLuZfmEgKqRg2y7KQvjG+61pvoz1OdmE2qyIiS72L2cXrFA==`; extensions `sha512-17eDCL6xE4XzpOX47rMt31RM/FYBRFrIHC1tuD4XlEoqcjVJMbEvzoKouXAMkrSQYMRUlBTKWn0cDN1SPhC5EQ==`. Direct directory/tar probes printed the exact canonical repository for all six manifests.
- Mandatory/current gates: typecheck and all release-script syntax checks exited `0`; pinned `npm ci`, lint, build, and test ran in order and exited `0` (`139` files / `1095` tests; unchanged 17 audit findings). Package smoke printed version `0.8.0` and help. `git diff --check` and empty staging exited `0`; no credentials, network/external state, commit, push, publish, Registry, or GitHub operation occurred.
- Round identities: contract `sha256:de2384d69df8c1f82e253da75cab4ee932b45bc17d822e239fbe5352780b97ad`; generator `sha256:c90a37b383f2fef77c2f83e2e02a40d3a573b6a454263b42924aa70db52fd948`; verifier `sha256:39cf1ff2443b20f50ecc4ee60d8e50bb83227cfa9ccea33b0fb8504aa8be3219`; integrated tests `sha256:80bbe74dc78639d8ef59108718bc961a5f30ea51ca4412034a141c755e2d066a`.

## Rework Executor Self-check Round 4 — TASK-01 provenance repository projection — executor-task01-rework-r4-20260810

- Provenance readiness: every accepted publishable manifest now supplies npm's exact public source-repository prerequisite with case preserved, and verifier acceptance covers both generated and uploaded bytes. The repository is part of candidate/tarball identity, so metadata cannot be altered after acceptance without detection.
- Single source of truth: only the canonical release contract contains the production repository literal. Root metadata is exact trusted input that must match it; generator and verifier both derive expected package manifests from it.
- Fail-closed coverage: root mismatch stops generation; directory absence/type/URL/casing mismatches stop policy acceptance; retained archive mismatch stops during explicit extracted-manifest verification even when archive integrity is recomputed. Existing containment, dependency/export, inventory, deterministic pack, and credential-isolation checks remain passing.
- Scope/handoff: all focused TASK-01, complete release, exact candidate/consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue evidence is current. Only TASK-01-authorized Round 4 paths changed, immutable records remain intact, and no executor verdict was written.

## Independent Quick Review Round 4 — reviewer-task01-quick-r4-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1 plus Amendment A1; Change Review Round 5 BF-1/confirmed CH-1; Rework Execution/Self-check Round 4; directly affected canonical metadata, deterministic candidate/archive identity, directory/archive acceptance, and npm provenance-readiness boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/release/package-contract.mjs` (`sha256:de2384d69df8c1f82e253da75cab4ee932b45bc17d822e239fbe5352780b97ad`), `scripts/build-release-packages.mjs` (`sha256:c90a37b383f2fef77c2f83e2e02a40d3a573b6a454263b42924aa70db52fd948`), `scripts/verify-release-packages.mjs` (`sha256:39cf1ff2443b20f50ecc4ee60d8e50bb83227cfa9ccea33b0fb8504aa8be3219`), repository regressions in `scripts/release-packages.test.mjs` (`sha256:80bbe74dc78639d8ef59108718bc961a5f30ea51ca4412034a141c755e2d066a`), root `package.json` (`sha256:03ea0dd05a34bed3c7ca82d541bf8078d8397f0a399ddd5d61767af7d336a3d3`), and generated manifest evidence (`sha256:4f1774dc44b04a74f4e44c25002f4948da024fbfbbdfb79f567a1b411f9ce2bc`).
- Commands/evidence inspected: Change Review Round 5 BF-1, Round 4 RED/GREEN/full/integration/gate evidence, current contract/generator/verifier/root manifest, focused test matrix, and direct generated-directory/archive probes. Reviewer freshly ran generator/verifier syntax checks, the deterministic generation plus hostile-root plus frozen-candidate policy cases (3/3 passed), exact retained-candidate verification (exit 0; joint candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, inventories `63/103/91`, one Zod, 12 runtime/12 type entries), direct reads of all six directory/archive manifests, complete diff whitespace validation, and empty-staging validation. No credentials, network/external state, Registry/GitHub mutation, commit, push, or publish operation occurred.
- Finding disposition: BF-1/CH-1 is resolved. `scripts/release/package-contract.mjs:9-13` owns one exact case-preserving repository object, and root `package.json:22-25` matches it. The generator rejects any root mismatch at `scripts/build-release-packages.mjs:168-173`, projects that contract object in `packageManifest` at lines 152-165, constructs marker-empty manifests before `treeIdentity` at lines 198-233, and writes marker-complete manifests before `npm pack` at lines 235-260. Repository metadata therefore contributes to the common deterministic candidate ID and to each retained tarball's SHA-512 SRI rather than being added after acceptance.
- Verification finding: `scripts/verify-release-packages.mjs:85-99,163-205` requires exact root and generated-directory manifests derived from the same contract; missing fields, wrong types, foreign URLs, and casing changes cannot compare equal. Its identity reconstruction at lines 181-219 includes the repository-bearing expected manifest. After archive-integrity and inventory checks, lines 229-255 safely extract each tarball, require the complete expected manifest again, and byte-compare every extracted entry with the accepted directory, so a repository-altered tarball is rejected even when its candidate record SRI is recomputed.
- Regression and provenance-readiness finding: `scripts/release-packages.test.mjs:39-75` proves the exact object in every generated directory and tarball. The contained root mismatch probe at lines 119-188 rejects altered casing without touching the real repository. The policy matrix at lines 191-272 rejects missing repository metadata, wrong type, foreign URL, wrong casing, and a repacked foreign-repository archive with recomputed SRI. Fresh direct probes confirmed the exact `git+https://github.com/Gloria-GK-406/miniAgent.git` value in all six accepted manifests and the new three SRIs recorded by the executor, satisfying the local case-preserving metadata prerequisite for provenance publication.
- Required remediation: none.
