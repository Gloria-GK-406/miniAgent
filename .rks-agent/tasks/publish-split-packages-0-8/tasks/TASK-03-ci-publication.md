# TASK-03 — Enforce identity-bound CI publication

## Task Unit Contract

- Contract revision: 1
- Goal and deliverables: Implement the credential-free publication state machine and GitHub workflow that validates pull requests, serializes reviewed master publication, retains the accepted candidate before mutation, publishes only exact missing tarballs core-first with provenance, and supports same-candidate partial retry and post-publication Registry smoke.
- Referenced spec requirements: RQ-009, RQ-010, RQ-011, RQ-012, RQ-014, RQ-015, RQ-017, RQ-018 plus final integrated coverage of RQ-001 through RQ-018.
- Referenced plan sections: T-004, T-005, T-006; BD-002, BD-004, BD-006; CK-007 through CK-013.
- Owned boundary: Registry lookup/identity decision and dry-run publication logic, pinned candidate recovery, joint identity partial-state checks, core visibility wait, npm provenance argv, workflow triggers/permissions/concurrency/secret scope/artifact retention, Registry-only consumer smoke, workflow policy fixtures, and complete local acceptance integration.
- Permitted files or interfaces: `scripts/publish-release-packages.mjs`, `scripts/registry-smoke.mjs`, workflow/publication-focused cases and fixtures under `scripts/`, `.github/workflows/ci.yml`, release npm scripts in `package.json`, and narrowly necessary corrections to TASK-01/TASK-02 owned files when their integrated interface is demonstrably inconsistent (record each overlap explicitly).
- Dependencies: TASK-02.
- Required dependency outputs: Accepted generator, candidate manifest/tarballs, verifier, clean-consumer probes, package metadata, and docs from TASK-01/TASK-02.
- Acceptance conditions: Only authoritative 404 means absent; indeterminate lookup fails closed; present packages must match SRI, source revision, and common candidate identity; invalid/mixed partial states fail; only accepted tarballs publish in core-engine-extensions order after core visibility; dry-run requires no token; workflow has fixed non-cancelling concurrency, pinned Node/npm packing, `contents: read`, required `actions: read`, `id-token: write`, step-only `NPM_TOKEN`, exact `npm publish <tgz> --provenance --access public`, validation-only PR behavior, and pre-mutation retained manifest/tarballs; post-run smoke can consume the retained record; root/CLI are never publish targets; all repository and release tests pass locally.
- Task-specific authorization: Root TaskContract authorization for local code/workflow implementation, read-only public Registry probes, and dry-run fixtures only. Do not push, invoke live `npm publish`, use `NPM_TOKEN`, or mutate GitHub/npm.
- Required methods: Use TDD for publication-state and workflow policy behavior. All local tests must explicitly remove `NODE_AUTH_TOKEN`; inject network/command operations for deterministic negative cases. Run final governed plan-drift check in a fresh context after Task acceptance.
- Required checks: Publication-policy and workflow tests; credential-free dry run; candidate build/verification; `npm run typecheck`; repository-mandated `npm run lint`, `npm run build`, `npm test` in order; package smoke where still applicable; complete diff/status/residue inspection.
- Required evidence: Appended changed paths, RED/GREEN results, state-matrix and workflow-policy results, candidate/consumer evidence, mandatory gate output, secret/residue inspection, executor self-check, matching passed quick review, and inputs sufficient for the final fresh drift review.

## Execution Result Round 1 — executor-task03-20260809

- Implemented `scripts/publish-release-packages.mjs` as an exact three-name, exact-version publication state machine over the TASK-02-verified frozen manifest/tarballs. Registry lookup uses the URL-encoded scoped-package/exact-version endpoint with bounded retries; only an authoritative `404` is absent, while auth/rate/server/network/malformed outcomes fail closed. Every present version must match name/version, SHA-512 SRI, source revision, and common candidate ID before any mutation decision.
- Implemented valid none/core/core+engine/core+extensions/all-present skip/retry behavior; rejects dependents without core, foreign sibling SRI/source/candidate identities, mismatched common markers, and perturbed accepted archives. Publication selection is the canonical core-engine-extensions order and only exact canonically contained retained `.tgz` paths are passed as `npm publish <tgz> --provenance --access public`. After any real/simulated publish, authoritative expected identity must become visible; an ambiguous outcome is accepted only when re-query proves the exact retained identity.
- Implemented `scripts/registry-smoke.mjs` for post-publication authoritative SRI/source/candidate/provenance checks and a credential-free Registry-only consumer that requests engine/extensions (not core), proves automatic core 0.8.0 plus no horizontal dependency and one physical Zod resolution, and runs all twelve runtime and strict NodeNext type entries.
- Replaced `.github/workflows/ci.yml` publication behavior with validation-only PRs, Node 20/22 repository checks, a Node 22.22.0/npm 10.9.4 candidate acceptance job, retry-first retained-artifact recovery keyed by SHA/run ID, fallback deterministic generation only when recovery is unavailable, complete exact-byte reacceptance, secret/config residue checks, a run ID/attempt/SHA/joint-ID/per-SRI record, and artifact upload before any Registry lookup/mutation. The master-only publish job depends on check and release-candidate, uses fixed non-cancelling `miniagent-npm-0.8.0` concurrency, permissions exactly `contents: read`, `actions: read`, `id-token: write`, downloads/reaccepts the same retained artifact, scopes `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` to the single publication step, then runs Registry smoke against the same manifest.
- Narrow integrated overlap: the TASK-01 generation case in `scripts/release-packages.test.mjs` now supports `--skip-generation`. Workflow recovery/generation completes before testing, then uses this switch so complete policy/docs/publication tests cannot overwrite a downloaded authoritative retry candidate. Normal local `release:test` still executes deterministic generation. This resolves a demonstrated TASK-01/TASK-03 retained-candidate interface inconsistency without changing generator behavior.
- Changed paths: `scripts/publish-release-packages.mjs`, `scripts/registry-smoke.mjs`, `scripts/fixtures/registry-none.json`, publication/workflow cases plus the narrow skip option in `scripts/release-packages.test.mjs`, `.github/workflows/ci.yml`, release script entries in `package.json`, and this Task evidence file. TASK-01/TASK-02 generator, verifier, manifests, package metadata, docs, and source behavior were reused unchanged.
- TDD RED: `fnm exec --using=22.22.0 node --test --test-name-pattern='publishes only|workflow retains' scripts/release-packages.test.mjs` exited `1`; decisive output: publication case failed with `Cannot find module .../scripts/publish-release-packages.mjs`, workflow case showed actual `needs: check` instead of required `[check, release-candidate]`, and summary was `tests 2`, `pass 0`, `fail 2`.
- TDD GREEN/focused result: after the minimum state machine/workflow implementation and completion of planned retry/perturbation cases, `fnm exec --using=22.22.0 node --test --test-name-pattern='publishes only|waits for core|perturbed|workflow retains' scripts/release-packages.test.mjs` exited `0`; decisive output: state matrix, candidate perturbation, ambiguous core visibility, and workflow/artifact policy all passed (`tests 4`, `pass 4`, `fail 0`).
- CK-007: `fnm exec --using=22.22.0 npm run release:test -- --publication-policy` exited `0` (`tests 9`, `pass 9`, `fail 0`). Covered valid none/one/two/all states, core plus either sibling, all-present idempotence, transient 5xx/network retry ending only on confirmed 404, 401/403/429/5xx/malformed/timeout failure, dependent-without-core, same-core/foreign-sibling SRI, source/common-candidate mismatch, candidate perturbation, ambiguous publish re-query, core visibility wait, exact order, and provenance argv.
- CK-008: `env -u NODE_AUTH_TOKEN fnm exec --using=22.22.0 npm run release:publish -- --candidate .release/candidate/manifest.json --dry-run --registry-fixture scripts/fixtures/registry-none.json` exited `0` and printed exactly three commands in core-engine-extensions order. Each command targets the absolute retained `archives/*.tgz` and ends `--provenance --access public`; no credential was required, read, or printed.
- CK-009: `fnm exec --using=22.22.0 npm run release:test -- --workflow` exited `0` (`9/9`). Parser/policy assertions prove PR non-mutation, master eligibility and gate dependencies, exact fixed concurrency/permissions/toolchain, one step-scoped secret reference, retained `.tgz` publisher provenance path, and Registry smoke.
- CK-010: `fnm exec --using=22.22.0 npm run release:test -- --workflow-artifact` exited `0` (`9/9`). Assertions prove retry download precedes fallback generation, generation is skipped after recovery, exact candidate reacceptance precedes run-record/upload, upload completes in a dependency job before mutation, and one SHA/run-ID artifact manifest feeds publication plus smoke; run attempt is retained for runner-loss/retry traceability.
- CK-011 mandatory order: `fnm exec --using=22.22.0 zsh -c 'npm ci && npm run lint && npm run build && npm test'` exited `0`. `npm ci` completed from lock (reporting the unchanged 17 audit findings: 2 low, 5 moderate, 10 high), lint reported `dependency architecture valid: src`, build completed `tsc`, and Vitest reported `139 passed` files / `1095 passed` tests.
- Package smoke: `fnm exec --using=22.22.0 npm run package:smoke` exited `0`; the retained local source/CLI product still reports `0.8.0` and renders its help. This is local compatibility evidence only; root/CLI are absent from the release allowlist and workflow publication targets.
- CK-012: `fnm exec --using=22.22.0 zsh -c 'npm run release:build -- --verify-determinism && npm run release:test && npm run release:verify -- --candidate .release/candidate/manifest.json'` exited `0`. Candidate remained `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; inventories are 63/103/91, release suite is 9/9, strict graph resolves one `zod@3.25.76`, and all twelve runtime plus twelve strict NodeNext entries pass.
- Required type/script checks: `fnm exec --using=22.22.0 npm run typecheck`, `node --check scripts/publish-release-packages.mjs`, and `node --check scripts/registry-smoke.mjs` each exited `0` without diagnostics.
- CK-013/residue inspection: `git diff --check` exited `0`; `git status --short` shows only the accumulated intended TASK-01/TASK-02/TASK-03 implementation/docs/workflow paths, generated `.release` remains ignored, and `git diff --cached --name-only` is empty. A scoped target/secret scan found the root aggregate name only in private source `package.json` and the sole CI secret reference only at the publication step; no direct root/CLI `npm publish`, credential file, token value, staged archive, or secret residue was found.

## Executor Self-check Round 1 — executor-task03-20260809

- State safety: every Registry version is independently classified before mutation; only exact 404 authorizes absence; present identity is immutable and exact; invalid partial/mixed candidate states stop before publication. Core is published and identity-visible before either dependent, and every publish/ambiguous outcome is followed by exact identity confirmation.
- Candidate safety: publisher first invokes the accepted TASK-02 policy verifier credential-free, enforces the canonical three-name allowlist, canonical retained `.tgz` containment, and exact manifest SRI/marker identity. Dry-run, fixtures, tests, artifact operations, and Registry lookup do not use npm authentication. Actual npm authentication can reach only the final workflow publication subprocess environment and is never inspected or logged by implementation code.
- Workflow safety: PRs cannot enter `publish`; master publication needs both repository and exact-candidate jobs; fixed concurrency cannot cancel/interleave; permissions omit write scopes other than required OIDC; artifact recovery is authoritative and precedes fallback; test execution cannot regenerate recovered bytes; the candidate/run record and upload precede mutation; publication and Registry smoke use the same downloaded record.
- Registry closeout readiness: smoke requires all three authoritative identities and provenance attestations, installs only engine/extensions from the fixed public Registry with empty owned npm configs and credential-scrubbed environment, observes automatic core, and exercises all public entries. It was not run against live 0.8.0 during local implementation because publication/Registry mutation and post-publication state are outside TASK-03 local authorization.
- Scope: no source capability, package contract, docs, candidate bytes, prior task evidence/review, SDD state, Git history, GitHub identity, remote, Registry, credential, token, commit, push, or publish operation was changed/accessed beyond the permitted local files and credential-free fixture/dry-run checks.
- Verification and handoff: all TASK-03 focused checks, mandatory repository gates, package smoke, deterministic candidate, complete release/consumer verification, type/script syntax, diff/status/staging/residue checks pass. The uncommitted complete result and exact outputs above are sufficient for independent TASK-03 quick review and the later fresh governed plan-drift review; no executor verdict was written.

## Independent Quick Review Round 1 — reviewer-task03-quick-20260809

- Verdict: `passed`.
- Reviewed contract: Task Unit Contract revision 1; referenced Spec RQ-009, RQ-010, RQ-011, RQ-012, RQ-014, RQ-015, RQ-017, RQ-018 and their directly associated acceptance/evidence clauses; Plan T-004 through T-006, BD-002, BD-004, BD-006, and CK-007 through CK-013; accepted TASK-02 dependency output through Independent Quick Review Round 3.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/publish-release-packages.mjs` (`sha256:df5b869061ed53a9d39f0aafbb9a8ac892168fe2e2eeb967dfb7fc1ffaf81c29`), `scripts/registry-smoke.mjs` (`sha256:90087a5243977acbe6efe18b8bf5960e837eea8fa51a3832429009f7742d2cd1`), publication/workflow plus integrated verification cases in `scripts/release-packages.test.mjs` (`sha256:4d1dff0d895b4f1b0d8a1378635a856fbdc9a04bc8c51d4f392ee7a0ff23d349`), `scripts/fixtures/registry-none.json` (`sha256:e9a3bf0c224c98525e3bae185ba23afc8ff94897549671d81f9ebfe371f991f0`), `.github/workflows/ci.yml` (`sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`), and release-script-bearing `package.json` (`sha256:03ea0dd05a34bed3c7ca82d541bf8078d8397f0a399ddd5d61767af7d336a3d3`).
- Commands/evidence inspected: executor Round 1 RED/GREEN, CK-007 through CK-013, package smoke, type/script checks, status/staging/residue records, current publisher/smoke/workflow/fixture implementation, and the accepted TASK-02 verifier interface. Reviewer freshly ran the credential-cleared publication/workflow suite (9/9 passed), credential-cleared CK-008 dry-run (exit 0; exactly core, engine, extensions retained absolute `.tgz` paths in order, each ending `--provenance --access public`), repository typecheck, both publication-script syntax checks, complete diff whitespace validation, and empty-staging validation.
- Findings: no obvious blocking defect was demonstrated in the bounded inputs. `scripts/publish-release-packages.mjs:60-125` treats only exact-version 404 as absent, retries then fails closed on indeterminate responses, and binds every present package to name/version/SRI/source revision/common candidate ID. `scripts/publish-release-packages.mjs:128-193` limits argv to the three canonically verified retained tarballs, enforces core-first visibility, handles same-candidate partial/ambiguous retry without overwrite, and emits exact provenance/access arguments; fixtures cover both valid two-package partial orders plus foreign/malformed/indeterminate states and candidate perturbation. `.github/workflows/ci.yml:3-122` keeps PRs validation-only, pins candidate/publication toolchains, retains or recovers and reaccepts one SHA/run-ID artifact before mutation, uses fixed non-cancelling publication concurrency and exact least privileges, gives the npm secret to only the publication step, and feeds the downloaded candidate to publication and Registry smoke. `scripts/registry-smoke.mjs:35-139` requires authoritative identities/provenance, installs only engine/extensions from the public Registry with owned empty npm configs, proves automatic core/no-horizontal/one-Zod resolution, and exercises all twelve runtime/type entries. The package target/secret scan exposes no root or CLI publication target and only the intended workflow secret reference.
- Required remediation: none.

## Rework Execution Result Round 2 — executor-task03-drift02-20260809

- Bounded drift remediation: replaced the post-publication provenance URL-presence check in `scripts/registry-smoke.mjs` with fail-closed npm/Sigstore attestation validation required by DRIFT-02 and approved Plan BD-008/T-009/CK-018. No publication state-machine, package source, Registry state, credential, workflow mutation behavior, or prior evidence/review was changed.
- Exact retrieval boundary: the smoke tool now fetches the exact `dist.attestations.url` only when it is an HTTPS, credential-free, query/fragment-free URL on `https://registry.npmjs.org` under `/-/npm/v1/attestations/`. Native requests send only `Accept: application/json`, reject redirects, use a 10-second abort, bounded attempts, and a 1 MiB streamed JSON limit. A 404 is missing and fails; malformed, oversized, auth/client, exhausted rate/server, and network outcomes all fail closed. Deterministic tests inject exact-URL fixture responses; `--metadata-only` is accepted only with that fixture and cannot suppress the real workflow consumer smoke.
- Payload and identity binding: the validator accepts the npm attestation-list envelope, supported Sigstore bundle media types v0.1/v0.2/v0.3, DSSE `application/vnd.in-toto+json`, and npm-supported SLSA provenance predicates v0.2/v1. It requires exactly one SLSA provenance attestation, a structurally valid non-empty signature envelope, matching outer/inner predicate type, exactly one in-toto v1 subject, and a subject `sha512` equal to the lowercase hex digest derived from the retained manifest SRI. SLSA v1 additionally requires the expected GitHub repository in `externalParameters.workflow.repository` plus a same-repository `resolvedDependencies` entry whose `gitCommit`/`sha1` is the retained `manifest.sourceRevision`; v0.2 requires the equivalent same-repository/revision `invocation.configSource` and `materials` binding. Repository normalization permits only credential-free GitHub HTTPS identity after canonical git syntax normalization.
- Deterministic regression matrix in `scripts/release-packages.test.mjs`: one exact valid retained attestation passes for each of the three packages. The focused case rejects an attestation endpoint 404, an empty list, malformed DSSE, wrong subject SHA-512, wrong repository, wrong source revision, duplicate/ambiguous provenance attestations, and a foreign-origin attestation URL. The existing workflow-policy assertion was strengthened to require the retained-candidate Registry smoke invocation to include `--require-provenance`; the workflow already did so and its bytes are unchanged this round (`sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`).
- Official format basis: implementation was checked against npm's official provenance documentation and `npm/provenance` compatibility/verification description (npm verifies the package PURL/tarball SHA-512 and supports Sigstore v0.1/v0.2 with SLSA v0.2/v1), the official Sigstore bundle/DSSE shape surfaced by GitHub's attestation API documentation, and the SLSA provenance v1 migration mapping for `resolvedDependencies` versus v0.2 `materials`/`configSource`. No third-party format source or live package state was used.
- TDD RED: `node --test --test-name-pattern='validates retained provenance' scripts/release-packages.test.mjs` exited `1` before implementation after `29.65s`; decisive failure was `Registry version did not become visible for @piaoxianguo/miniagent-core`, demonstrating that the old smoke tool ignored the exact injected Registry/attestation responses and performed only its live URL-presence behavior.
- TDD GREEN/focused: `fnm exec --using=22.22.0 node --test --test-name-pattern='validates retained provenance|workflow retains' scripts/release-packages.test.mjs` exited `0` (`tests 2`, `pass 2`, `fail 0`). The provenance-only run also exited `0` in `0.22s`, proving all valid/invalid fixtures complete without network or retry sleeps.
- TASK-03 publication/workflow suite: after regenerating with the required pinned toolchain, `fnm exec --using=22.22.0 npm run release:test -- --workflow` exited `0` (`11/11`), and the final complete `fnm exec --using=22.22.0 npm run release:test` exited `0` (`11/11`). Publication partial-state/order/provenance argv, candidate perturbation, ambiguous visibility, workflow retention, credential isolation, consumer/docs policy, and the new attestation matrix all pass together.
- Candidate/integrated verification: `fnm exec --using=22.22.0 npm run release:build -- --verify-determinism` exited `0` and regenerated the unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`. `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` exited `0`, retained inventories `63/103/91`, one `zod@3.25.76`, the required dependency graph, and all twelve runtime plus twelve type entries. `fnm exec --using=22.22.0 npm run typecheck` and `node --check scripts/registry-smoke.mjs` exited `0` without diagnostics.
- Mandatory gates: in required order, pinned `npm run lint`, `npm run build`, and `npm test` all exited `0`; dependency architecture was valid and Vitest reported `139 passed` files / `1095 passed` tests. `fnm exec --using=22.22.0 npm run package:smoke` exited `0` with CLI version `0.8.0` and help output.
- Changed paths in this round: `scripts/registry-smoke.mjs` (`sha256:c0cc60a513f9aea55f9a7b90a72a7695c3aa7ba5d5fd32ffd56d13ac88e767db`), provenance/workflow regression additions in `scripts/release-packages.test.mjs` (`sha256:1442bd216713128c441ff041df3f23d18423ab3b6e4fea2790c26583b51e382d`), and this appended TASK-03 evidence. The post-publish workflow invocation was verified but not edited in this round.
- Residue inspection: `git diff --check` exited `0`; `git diff --cached --name-only` is empty. A scoped scan of `scripts/registry-smoke.mjs` found no literal authorization header, `NODE_AUTH_TOKEN`, `NPM_TOKEN`, or `_authToken`; the network path does not read or forward caller credential values. `git status --short` contains only the accumulated intended TASK-01/TASK-02/TASK-03 paths already in the shared uncommitted worktree; `.release` remains ignored. No commit, push, publish, live Registry mutation, token lookup, credential inspection, or SDD state edit occurred.

## Executor Self-check Round 2 — executor-task03-drift02-20260809

- DRIFT-02 closure: post-publish success can no longer be inferred from an attestation URL. Each retained package must supply one parseable npm/Sigstore SLSA provenance statement whose subject is the exact retained archive digest and whose authoritative GitHub source context and commit are the retained repository/revision; missing, malformed, foreign, wrong, and ambiguous evidence fails the job.
- Fail-closed/network safety: both metadata and attestations use the same bounded credential-free client. The attestation request cannot redirect or cross Registry origin, response parsing is size/time/attempt bounded, and no response/error path logs body data, environment secrets, headers, or credential values. Fixture-only metadata mode is explicitly gated and absent from the workflow.
- Workflow/integration safety: `.github/workflows/ci.yml` still invokes `release:registry-smoke` after publication against the downloaded retained manifest with `--require-provenance`, so real post-publish execution performs identity-bound attestation validation before its Registry-only consumer checks. Existing core-first publication, exact tarball, provenance argv, concurrency, permissions, artifact-before-mutation, and step-scoped npm token policies remain covered and passing.
- Scope and handoff: only the TASK-03-permitted smoke/test/evidence paths were modified. Prior execution/review/drift records and SDD state are intact; no verdict was written. Fresh pinned focused, complete release, candidate/consumer, type, lint/build/test, package smoke, syntax, whitespace, staging, and residue evidence is recorded above and ready for independent quick review.

## Independent Quick Review Round 2 — reviewer-task03-quick-r2-20260809

- Verdict: `changes_required`.
- Reviewed contract: Task Unit Contract revision 1; DRIFT-02 and Rework Round 2 only; approved Plan BD-008, T-009 step 1, and CK-018, plus the unchanged workflow invocation required to exercise the remediation.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/registry-smoke.mjs` (`sha256:c0cc60a513f9aea55f9a7b90a72a7695c3aa7ba5d5fd32ffd56d13ac88e767db`), provenance/workflow regressions in `scripts/release-packages.test.mjs` (`sha256:1442bd216713128c441ff041df3f23d18423ab3b6e4fea2790c26583b51e382d`), and unchanged `.github/workflows/ci.yml` (`sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`).
- Commands/evidence inspected: DRIFT-02, Rework Round 2 RED/GREEN, provenance matrix, complete release/candidate/type/mandatory-gate/package-smoke/syntax/residue evidence, current smoke/test/workflow implementation, and official npm/Sigstore bundle descriptions. Reviewer reran the provenance/workflow focused cases (2/2 passed), repository typecheck (exit 0), smoke-script syntax (exit 0), owned whitespace checks (exit 0), and empty-staging inspection.
- Confirmed remediation: attestation retrieval is exact-origin HTTPS, credential-free, redirect-free, query/fragment-free, limited to the npm attestation path, bounded to 10 seconds/three attempts/1 MiB, and fails closed on missing or indeterminate responses at `scripts/registry-smoke.mjs:147-218` and `240-258`. The decoded in-toto statement requires one subject whose lowercase SHA-512 equals the retained SRI digest and binds the expected GitHub repository plus retained source revision for supported SLSA v0.2/v1 shapes at `scripts/registry-smoke.mjs:78-145`; wrong digest/repository/revision, foreign URL, missing evidence, and duplicate supported provenance are rejected. `.github/workflows/ci.yml:121-122` invokes the retained candidate smoke with `--require-provenance`, and the workflow regression asserts the flag.
- Finding: malformed Sigstore bundles are accepted as provenance. `validateProvenance` at `scripts/registry-smoke.mjs:125-134` requires only that `verificationMaterial` is any object and that the DSSE signature array is nonempty with nonempty `sig` strings. It does not require actual certificate/public-key verification material, exactly one DSSE signature, or canonical Base64 signature bytes. The accepted positive fixture at `scripts/release-packages.test.mjs:414-425` explicitly uses `verificationMaterial: {}` and `sig: "fixture-signature"` (not Base64), proving the focused suite passes an invalid Sigstore bundle. The nominal malformed-DSSE negative at `scripts/release-packages.test.mjs:467` lacks a supported outer `predicateType`, so it is rejected as missing provenance before exercising DSSE payload/signature validation. Official Sigstore bundle requirements state that verification material supplies the key material for the signature and that an attestation DSSE envelope contains exactly one Base64 signature. Therefore `--require-provenance` still does not safely establish a parseable npm/Sigstore attestation, even though its untrusted statement fields match the retained candidate.
- Required remediation: validate the accepted npm/Sigstore bundle structure fail closed—at minimum supported, nonempty certificate/public-key verification material, exactly one canonical Base64 DSSE signature, and a canonical Base64 DSSE payload—then replace the positive fixture with a structurally valid representative bundle and add reached-path negatives for empty/malformed verification material, invalid signature encoding, multiple signatures, and malformed payload under an otherwise supported attestation. If CK-018 is intended to claim cryptographic signature/provenance verification rather than authoritative Registry field binding, use npm/Sigstore verification rather than merely parsing signature material. Rerun the provenance/workflow focused suite, complete release checks, type/syntax, and residue checks, then request a fresh independent quick-review round.

## Rework Execution Result Round 3 — executor-task03-sigstore-structure-20260809

- Bounded remediation: strengthened only the Sigstore bundle structural validation identified by Independent Quick Review Round 2. `scripts/registry-smoke.mjs` now recognizes the official legacy `application/vnd.dev.sigstore.bundle+json;version=0.1|0.2|0.3` media forms and current `application/vnd.dev.sigstore.bundle.v0.3+json`; rejects all other bundle media types; and applies the official media-version-specific keyless verification-material shape.
- Verification material: v0.1/v0.2 require the `x509CertificateChain` oneof with at least one certificate; v0.3 requires the single `certificate` oneof. Exactly one verification-material content alternative may be present. Every `rawBytes` value must be nonempty canonical Base64, decode within the existing 1 MiB bound, parse as an actual X.509 certificate, expose a public key, and not be a CA certificate. Empty objects, wrong version/form combinations, non-Base64 material, canonical Base64 garbage that is not X.509, empty chains, and multiple oneof alternatives fail closed.
- DSSE envelope: supported attestations now require exactly one signature object, never merely a nonempty array. Both its `sig` and the statement `payload` pass one shared strict decoder requiring nonempty padded canonical standard Base64 with exact decode/re-encode equality and bounded decoded length. Empty signature arrays, empty signature values, invalid/noncanonical signature encodings, multiple signatures, invalid payload encodings, and whitespace/noncanonical payload encodings are rejected before statement identity checks.
- Scope boundary: this is deliberately structural Registry-published attestation validation plus the Round 2 retained subject/source bindings. It does not claim or attempt full cryptographic signature, trust-root, transparency-log, certificate-time, or OIDC identity verification, which was not authorized or required by the bounded rework. The existing exact-origin credential-free Registry fetch and workflow invocation are unchanged.
- Fixture changes: the positive v0.3 fixture now carries a statically embedded parseable CA:false X.509 DER certificate and a canonical Base64 signature; positive v0.1 and v0.2 cases exercise the required certificate-chain form for all three packages. Reached-path negatives cover empty and malformed verification material, canonical garbage certificate bytes, v0.2 using the wrong v0.3 certificate form, zero signatures, empty signature bytes, non-Base64 signature, multiple signatures, invalid payload Base64, and noncanonical payload Base64, in addition to the Round 2 identity/source/ambiguity cases.
- TDD RED: `fnm exec --using=22.22.0 node --test --test-name-pattern='validates retained provenance' scripts/release-packages.test.mjs` exited `1` (`tests 1`, `pass 0`, `fail 1`) in `0.13s`; decisive output was `invalid attestation passed` with `"provenance": "verified"`. By fixture order this was the otherwise supported attestation whose `verificationMaterial` was `{}`, directly reproducing the reviewer finding before production changes.
- TDD GREEN/focused: after the minimum parser changes and structural fixture completion, `fnm exec --using=22.22.0 node --test --test-name-pattern='validates retained provenance|workflow retains' scripts/release-packages.test.mjs` exited `0` (`tests 2`, `pass 2`, `fail 0`) in `0.45s`. The final provenance-only run exited `0` in `0.44s`, including positive v0.1/v0.2/v0.3 and every new negative case without network access.
- Complete TASK-03/release checks: final `fnm exec --using=22.22.0 npm run release:test` exited `0` (`11/11`) in `22.66s`. `fnm exec --using=22.22.0 npm run release:verify -- --candidate .release/candidate/manifest.json` exited `0` with unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`, inventories `63/103/91`, one `zod@3.25.76`, and all twelve runtime plus twelve type entries. `fnm exec --using=22.22.0 npm run typecheck` and `node --check scripts/registry-smoke.mjs` exited `0` without diagnostics.
- Mandatory/current repository gates: pinned `npm run lint`, `npm run build`, and `npm test` ran in required order and exited `0`; dependency architecture was valid, and Vitest reported `139 passed` files / `1095 passed` tests. A final pinned `npm run package:smoke` exited `0`, printing CLI version `0.8.0` and help.
- Round-owned path identities: `scripts/registry-smoke.mjs` is `sha256:25199723af6e878f31548947d25c51b207c2300a62a749c31996508b9c82a165`; `scripts/release-packages.test.mjs` is `sha256:4d162c4e96650bda4022ec25c5690ec6f2609046ade6bae589eb22473fb4fbab`; unchanged `.github/workflows/ci.yml` remains `sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`; and this TASK-03 evidence was appended. No other Round 3 implementation path changed.
- Residue/safety: final `git diff --check` and the empty-staging assertion exited `0`. The credential-term scan of `scripts/registry-smoke.mjs` remains empty. No live Registry lookup/mutation, npm publish, token/credential access, commit, push, GitHub identity action, prior evidence/review/drift edit, or SDD state edit occurred.

## Executor Self-check Round 3 — executor-task03-sigstore-structure-20260809

- Finding closure: an empty `verificationMaterial`, plain-text signature, or multiple-signature envelope can no longer pass. Supported bundle versions must carry the keyless certificate form required by the official Sigstore bundle schema, and every certificate, signature, and payload encoding is structurally parsed rather than accepted as a truthy string/object.
- Fail-closed coverage: the positive matrix proves v0.1/v0.2 chain and v0.3 certificate handling; the negative matrix independently reaches material, certificate, signature cardinality/value, and payload decoders under an otherwise supported npm/SLSA attestation. The retained SHA-512 subject, expected GitHub repository, exact source revision, same-origin URL, bounded fetch/body, and ambiguity requirements remain intact.
- Claim calibration: Registry smoke establishes a structurally valid Registry-published Sigstore envelope with statement identity/source binding. It intentionally does not label that structural check as standalone cryptographic Sigstore verification; full trust-root/signature verification remains npm/Sigstore verifier territory and outside this rework authorization.
- Workflow/scope: the post-publish workflow still supplies `--require-provenance` against the retained candidate, so these stricter checks run before the Registry-only consumer probes. Only permitted smoke/test/evidence files changed, all final gates are current, prior rounds remain immutable, and the result is ready for a fresh independent quick review without an executor verdict.

## Independent Quick Review Round 3 — reviewer-task03-quick-r3-20260809

- Verdict: `changes_required`.
- Reviewed contract: Task Unit Contract revision 1; DRIFT-02, Independent Quick Review Round 2's Sigstore-structure finding, and Rework Execution/Self-check Round 3 only; approved provenance boundary BD-008, T-009 step 1, and CK-018. The review treated full cryptographic signature/trust-root/transparency verification as explicitly outside this bounded remediation.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/registry-smoke.mjs` (`sha256:25199723af6e878f31548947d25c51b207c2300a62a749c31996508b9c82a165`), provenance/workflow regressions in `scripts/release-packages.test.mjs` (`sha256:4d162c4e96650bda4022ec25c5690ec6f2609046ade6bae589eb22473fb4fbab`), and unchanged `.github/workflows/ci.yml` (`sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`).
- Commands/evidence inspected: complete TASK-03, DRIFT-02, Round 2 finding, Round 3 execution/self-check, current smoke/test implementation, the official Sigstore `sigstore_bundle.proto` verification-material and DSSE cardinality definitions, and current workflow invocation. Reviewer freshly ran the focused provenance/workflow cases (2/2 passed), repository typecheck (exit 0), smoke-script syntax check (exit 0), and owned whitespace checks (no diagnostics). A Node 22 fixture generated an OpenSSL-verified CA-to-leaf chain and supplied it through an otherwise valid v0.2 retained provenance response; Registry smoke exited 1 with `Malformed Sigstore verification material` even though OpenSSL verified the leaf against the included CA and Node parsed the first certificate as `CA:false` and its issuer as `CA:true`.
- Finding 1: supported v0.1/v0.2 certificate chains are over-rejected at `scripts/registry-smoke.mjs:87-115`. `certificates.every(validCertificate)` applies `!certificate.ca` to every chain member, but the Sigstore schema requires only the first member to be the leaf and permits subsequent issuing certificates; it explicitly discusses intermediate/root CA members. Consequently a structurally correct, parseable leaf-plus-issuer chain is rejected, so the claimed supported v0.1/v0.2 verification-material handling is not compatible with the accepted bundle structure. The current positive legacy fixtures at `scripts/release-packages.test.mjs:468-475` contain only one leaf certificate and cannot expose this regression.
- Finding 2: the Round 3 negative evidence is incomplete relative to its stated guarantees. `scripts/release-packages.test.mjs:480-514` covers a noncanonical payload and a malformed signature, but has no noncanonical-yet-decodable signature fixture and no verification-material fixture containing multiple oneof alternatives. Thus the suite does not demonstrate the claimed noncanonical-signature and oneof-cardinality rejection paths even though the implementation appears intended to reject them.
- Finding 3: claim calibration remains inconsistent at `scripts/registry-smoke.mjs:301-303`. Metadata-only success prints `"provenance": "verified"` without limiting that result to structural parsing and retained subject/source binding. Because this remediation deliberately performs no signature, trust-root, certificate-time, transparency-log, or OIDC verification, the externally recorded result overstates the approved boundary and contradicts the Round 3 self-check's no-cryptographic-verification claim.
- Required remediation: accept and structurally parse valid v0.1/v0.2 chains by requiring the first certificate to be a non-CA leaf while handling subsequent parseable issuer certificates consistently with the supported schema; add a valid leaf-plus-issuer regression. Add reached-path negatives for a noncanonical Base64 signature and multiple verification-material oneof alternatives. Replace or qualify the metadata result so it reports structural/identity/source binding rather than unqualified provenance verification. Preserve the exact subject digest and repository/revision matrix for v0.1/v0.2/v0.3, the bounded credential-free fetch, and the explicit no-full-crypto scope; rerun the focused provenance/workflow, complete release, type/syntax, and residue checks before a fresh quick review.

## Rework Execution Result Round 4 — executor-task03-chain-claims-20260809

- Bounded remediation: fixed only the three findings from Independent Quick Review Round 3 in `scripts/registry-smoke.mjs`, its deterministic provenance fixtures in `scripts/release-packages.test.mjs`, and this appended evidence. Exact retained subject/source identity, credential-free bounded Registry retrieval, workflow invocation, and publication behavior remain unchanged.
- Legacy certificate-chain handling: v0.1/v0.2 chains now parse the first entry as a required CA:false leaf with a public key, while subsequent entries are parsed as issuer certificates and may/must be CA:true. Each adjacent leaf/intermediate-to-issuer pair must satisfy Node X.509 `checkIssued` and verify the child's certificate signature with the next issuer's public key. This accepts a correctly ordered leaf-plus-issuer chain, rejects a CA in the leaf position, non-CA or malformed issuer entries, unrelated/out-of-order issuers, and broken chain signatures. v0.3 continues to require one CA:false leaf certificate rather than a chain.
- Real chain fixture: the positive v0.1/v0.2 fixtures now contain a deterministic CA:false leaf certificate followed by its CA:true issuer. The pair was generated as a test-only X.509 chain and is accepted only after both issuer naming and signature relationship checks; the earlier single self-signed leaf fixture was replaced. All three retained package responses exercise this chain for both legacy media versions.
- Reached negative branches: added `AR==` as a noncanonical-but-decodable signature value; Node decodes it, but canonical re-encoding is `AQ==`, so the strict equality branch rejects it. Added a supported v0.3 material object containing both `certificate` and `publicKey` alternatives, which reaches and rejects the oneof-cardinality branch. Existing malformed signature, zero/empty/multiple signature, payload, certificate, source, and subject cases remain.
- Claim calibration: fixture-only metadata output no longer emits the unqualified `"provenance": "verified"`. It now emits exactly `provenanceStatement: "identity-bound"` and `cryptographicVerification: "not-performed"` alongside the retained candidate ID, and the positive regression asserts the whole JSON object. This reports only the structural statement/subject/source result; trusted post-release provenance may be established separately by npm/GitHub's cryptographic provenance mechanisms. A scoped scan of executable tests and user docs found no remaining unqualified `provenance ... verified` claim; immutable prior review/evidence quotations were not edited and are superseded by this round's precise output.
- TDD RED cycle 1: after adding the exact output assertion, `fnm exec --using=22.22.0 node --test --test-name-pattern='validates retained provenance' scripts/release-packages.test.mjs` exited `1` (`1/1` failed) in `0.06s`; the diff showed actual `{ provenance: "verified" }` versus required `{ provenanceStatement: "identity-bound", cryptographicVerification: "not-performed" }`.
- TDD RED cycle 2: after the minimum output-label correction and before chain implementation, the same focused command exited `1` (`1/1` failed) in `0.08s`; decisive output was `Sigstore 0.1: ... Malformed Sigstore verification material`, proving that the valid CA-to-leaf legacy fixture reproduced the chain over-rejection independently of the output issue.
- TDD GREEN/focused: after ordered issuer parsing, `fnm exec --using=22.22.0 node --test --test-name-pattern='validates retained provenance' scripts/release-packages.test.mjs` exited `0` (`1/1`) in `0.49s`. Final `fnm exec --using=22.22.0 node --test --test-name-pattern='validates retained provenance|workflow retains' scripts/release-packages.test.mjs` exited `0` (`2/2`) in `0.49s`, covering exact output, both legacy chains, current v0.3, new negatives, and unchanged workflow provenance invocation.
- Complete release/integration: final pinned `npm run release:test` exited `0` (`11/11`) in `26.90s`. Pinned `npm run release:verify -- --candidate .release/candidate/manifest.json` exited `0` with unchanged candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`, inventories `63/103/91`, one `zod@3.25.76`, and all twelve runtime plus twelve type entries. Pinned `npm run typecheck` and `node --check scripts/registry-smoke.mjs` exited `0` without diagnostics.
- Mandatory/current repository gates: pinned `npm run lint`, `npm run build`, and `npm test` ran in required order and exited `0`; dependency architecture was valid and Vitest reported `139 passed` files / `1095 passed` tests. Final pinned `npm run package:smoke` exited `0` with CLI version `0.8.0` and help.
- Round-owned identities/residue: `scripts/registry-smoke.mjs` is `sha256:035958c8aef45a7274359a882985f33aa3c32b4635e8344c39e9be343f1c86cc`; `scripts/release-packages.test.mjs` is `sha256:686ff926ece7b1ceb9125de39c2a79bcfcdc886f52d4b8b5935e0bbd14415d4d`; unchanged `.github/workflows/ci.yml` remains `sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`. `git diff --check` and empty staging assertions exited `0`; status contains only accumulated intended TASK paths. No live Registry request/mutation, credential/token access, publish, commit, push, prior evidence/review/drift edit, or SDD state edit occurred.

## Executor Self-check Round 4 — executor-task03-chain-claims-20260809

- Chain correctness: legacy verification material now follows the Sigstore ordering rule—leaf first, issuers afterward—without incorrectly requiring intermediates/roots to be non-CAs. Parseability, CA role, issuer identity, and adjacent signature relations are all checked; v0.3 remains leaf-only.
- Evidence completeness: the suite contains both a cryptographically related valid leaf-plus-issuer fixture and reached-path negatives for canonicalization and oneof cardinality. `AR==` specifically distinguishes permissive decoding from canonical encoding, while the dual `certificate`/`publicKey` fixture proves the cardinality check rather than failing in unrelated parsing.
- Claim boundary: output and tests state only that the provenance statement is structurally identity-bound and explicitly state that cryptographic verification was not performed. No implementation or current documentation claims that Registry smoke independently verifies Sigstore trust roots, DSSE signatures, transparency inclusion, certificate time, or OIDC identity.
- Scope/handoff: only permitted TASK-03 smoke/test/evidence paths changed, all current focused/full/mandatory/residue gates pass, earlier rounds remain immutable, and the uncommitted result is ready for a fresh independent quick review without an executor verdict.

## Independent Quick Review Round 4 — reviewer-task03-quick-r4-20260809

- Verdict: `passed`.
- Reviewed contract: Task Unit Contract revision 1; DRIFT-02; Independent Quick Review Round 3 findings; Rework Execution/Self-check Round 4; approved provenance boundary BD-008, T-009 step 1, and CK-018. Full DSSE/trust-root/transparency/OIDC cryptographic verification remained explicitly outside this bounded remediation.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/registry-smoke.mjs` (`sha256:035958c8aef45a7274359a882985f33aa3c32b4635e8344c39e9be343f1c86cc`), provenance/workflow regressions in `scripts/release-packages.test.mjs` (`sha256:686ff926ece7b1ceb9125de39c2a79bcfcdc886f52d4b8b5935e0bbd14415d4d`), unchanged `.github/workflows/ci.yml` (`sha256:1e052f4c9d4a8c78a7478eb3d5c95d03dad78d9a967984b7e690f698d2d664d0`), and generated evidence `.release/candidate/manifest.json` (`sha256:adb607a5a6f77391ceab3b145013eb435d20daf1779ad10e4172fc1fd0aa4dd6`).
- Commands/evidence inspected: complete TASK-03 recent execution/review evidence and current smoke/tests/workflow; reviewer reran the focused provenance/workflow cases (2/2 passed), complete release suite (11/11 passed), repository typecheck (exit 0), smoke-script syntax (exit 0), owned whitespace checks (exit 0), and empty-staging assertion. An independent Node X.509 probe of the embedded legacy fixture observed leaf `CA:false`, issuer `CA:true`, matching leaf issuer/issuer subject, `checkIssued: true`, and leaf certificate signature verification with the issuer public key `true`.
- Findings: no obvious blocking defect was demonstrated. Legacy v0.1/v0.2 handling at `scripts/registry-smoke.mjs:103-130` requires one chain alternative, parses a non-CA leaf first, permits only CA issuers afterward, and verifies each adjacent issuance and certificate-signature relation; the valid leaf-plus-issuer fixture is exercised for every retained package under both legacy media versions at `scripts/release-packages.test.mjs:415-483`. Current v0.3 remains constrained to exactly one non-CA leaf certificate at `scripts/registry-smoke.mjs:107-132`. The reached `AR==` negative proves decodable but noncanonical DSSE signature rejection, and the dual `certificate`/`publicKey` fixture proves verification-material oneof-cardinality rejection at `scripts/release-packages.test.mjs:507-527`.
- Findings continued: metadata-only success now reports exactly `provenanceStatement: "identity-bound"` and `cryptographicVerification: "not-performed"` at `scripts/registry-smoke.mjs:318-324`, with an exact-object regression at `scripts/release-packages.test.mjs:467-473`; no current executable/docs scan match overclaims full provenance cryptographic verification. Exact same-origin credential-free bounded retrieval, retained SHA-512 subject binding, expected repository/source revision binding, missing/malformed/wrong/foreign/ambiguous rejection, and workflow `--require-provenance` invocation remain intact. Complete release testing confirms no generator/verifier/publication/workflow regression.
- Required remediation: none.

## Rework Execution Result Round 5 — executor-task03-final-review-20260809

- Bounded remediation: addressed only final change-review BF-1/BF-2 and reversals CH-1/CH-2/CH-3 in the permitted workflow, publication scripts, machine-readable release intent, regression suite, and this appended evidence. Prior evidence/reviews/challenges/drift/state, candidate bytes, credentials, and Registry state were not changed.
- One-shot eligibility: `scripts/release-intent-0.8.0.json` records exact version `0.8.0` and the ordered core/engine/extensions names. `scripts/release-eligibility.mjs` accepts only a master push whose exact 40-hex `github.sha` commit adds (`A`) that exact marker relative to its parent. Same-SHA retries remain eligible; a later commit where the marker is merely present returns `eligible:false`. Workflow candidate retention and publication both require this output while fixed noncancel concurrency, SHA/run-ID artifact recovery, least permissions, and publication-step-only token scope remain unchanged.
- Reversal regression: a temporary Git history evaluates later candidate B before eligible A, proves B contributes no modeled Registry operation, accepts A, then rejects B again. Thus readiness order and later master pushes cannot select another release mutation run.
- Exact Registry/npm boundary: native lookup is fixed to `https://registry.npmjs.org`, rejects redirects, and remains bounded/fail-closed. Before lookup, the publisher rejects caller registry overrides and caller/repository/candidate `.npmrc` files without reading them. Npm receives exactly `publish <canonical accepted tgz> --provenance --access public --registry https://registry.npmjs.org/` from a canonical owned temporary cwd, with owned empty global config and a user config containing only the Registry and literal `${NODE_AUTH_TOKEN}` mapping. Its allowlisted environment preserves required publication/OIDC context while replacing npm config paths and removing redirect/auth/OTP/tag/material caller config.
- No-network npm regression: an injected Node wrapper proves all three mutations receive exact argv/tarballs/cwd/config, the empty global file, literal token mapping, required publication/OIDC key presence, and no other `npm_config_*` material. Hostile external user config, auth/tag variables, registry redirect, and project `.npmrc` fail before wrapper invocation; dry-run also fails closed on redirect. No network or Registry mutation occurred.
- TDD RED: the focused eligibility/boundary/workflow command exited `1` (`0/3`): the eligibility module was absent, the old publisher rejected the injected command fixture, and the workflow lacked the gate. TDD GREEN: identical focused cases plus syntax checks exited `0` (`3/3`, `2.37s`). Final pinned `npm run release:test` exited `0` (`13/13`, `29.47s`).
- Integrated/current gates: pinned deterministic release build retained candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; release verification exited `0` with inventories `63/103/91`, one `zod@3.25.76`, required graph, and 12 runtime/12 type entries. Typecheck and all three script syntax checks exited `0`. Pinned `npm ci`, lint, build, and test ran in required order and exited `0` (`139` files / `1095` tests; unchanged 17 audit findings). Package smoke, `git diff --check`, and empty staging exited `0`.
- Round identities: publisher `sha256:62dbbcae5e59f776029f17071b60fd95fbb3c566fa8450cc74eb018679a31dfc`; eligibility `sha256:ced81618dc378241baeb2f8fe9e6d3e2a4544eabbb11107f25bb1f9af7b427c1`; marker `sha256:075ea0d2fdddbb409eff26c904f397d013cd9ed3f036022c7340d536f534535f`; tests `sha256:aa9e290a63b59cabdb0cf0a7d8465662ad4c8fed3353387ca46d95ccfd7592c0`; workflow `sha256:c5d45924e14a13a0d642d6725389db78f499a928f0d6f9708f3dba1f633561a1`. Secret scan found the sole workflow secret reference only in the publication step plus the publisher's required literal mapping/key allowlist; no value was read/logged. No GitHub identity, credential, commit, push, publish, or live Registry action occurred.

## Executor Self-check Round 5 — executor-task03-final-review-20260809

- BF-1/CH-1/CH-3: eligibility is now a property of one exact commit, independent of readiness timing; later commits cannot re-enter, while same-SHA recovery retries remain valid and ineligible release jobs skip cleanly.
- BF-2/CH-2: lookup and mutation use one literal npmjs origin with redirect following disabled, explicit npm Registry argv, owned cwd/config, and hostile caller/project material excluded. The no-network child-boundary regression directly observes these guarantees.
- Candidate/mutation safety: credential-free accepted-candidate verification, canonical archive containment, exact-tarball/core-first order, and same-candidate ambiguous-outcome confirmation remain intact. Injected wrapper failure is fail-closed rather than treated as a real ambiguous npm outcome.
- Scope/handoff: only authorized TASK-03 implementation/test/evidence paths changed. Focused, complete release, deterministic candidate, consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue checks are current; no executor verdict was written.

## Independent Quick Review Round 5 — reviewer-task03-quick-r5-20260809

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; immutable challenges CH-1, CH-2, and CH-3; final change-review BF-1 and BF-2; Rework Execution/Self-check Round 5; directly affected RQ-009, RQ-015, RQ-017, RQ-018 and Plan T-004/T-005 publication eligibility, same-authority, credential, retry, and concurrency boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/publish-release-packages.mjs` (`sha256:62dbbcae5e59f776029f17071b60fd95fbb3c566fa8450cc74eb018679a31dfc`), `scripts/release-eligibility.mjs` (`sha256:ced81618dc378241baeb2f8fe9e6d3e2a4544eabbb11107f25bb1f9af7b427c1`), `scripts/release-intent-0.8.0.json` (`sha256:075ea0d2fdddbb409eff26c904f397d013cd9ed3f036022c7340d536f534535f`), BF-1/BF-2 regressions in `scripts/release-packages.test.mjs` (`sha256:aa9e290a63b59cabdb0cf0a7d8465662ad4c8fed3353387ca46d95ccfd7592c0`), and `.github/workflows/ci.yml` (`sha256:c5d45924e14a13a0d642d6725389db78f499a928f0d6f9708f3dba1f633561a1`).
- Commands/evidence inspected: complete BF-1/BF-2 findings, immutable CH-1/CH-2/CH-3, Round 5 RED/GREEN/full/gate/residue evidence, current eligibility/publisher/workflow/marker and focused regressions. Reviewer freshly ran the three BF-1/BF-2/workflow cases (3/3 passed), eligibility and publisher syntax checks, complete diff whitespace validation, empty-staging validation, exact identity hashes, and a scoped secret/Registry/publish scan. All checks completed without diagnostics; no live lookup, external mutation, credential access, commit, push, or publish occurred.
- BF-1/CH-1/CH-3 disposition: resolved. `scripts/release-eligibility.mjs:29-53` requires a master push, exact GitHub SHA shape, exact tracked marker content, available parent, and an `A` status for that marker in the supplied SHA's parent diff. `.github/workflows/ci.yml:31-53` evaluates that gate before candidate creation, while both candidate and publish jobs require its exact `true` output at `.github/workflows/ci.yml:50-53,105-108`. The temporary-history regression evaluates later B before A, records no modeled Registry operation for B, accepts A, and rejects B again; the same A SHA remains eligible on retry. Fixed non-cancelling version concurrency and SHA/run-ID artifact recovery remain intact.
- BF-2/CH-2 disposition: resolved. `scripts/publish-release-packages.mjs:12-13,53-92,131-176,193-227,257-281` binds lookup and mutation to the same literal npmjs origin, disables fetch redirects, rejects caller Registry overrides and caller/repository/candidate `.npmrc` presence before lookup, and invokes npm from one canonical owned temporary directory with owned user/global configs. Each child receives exactly `publish <canonical accepted .tgz> --provenance --access public --registry https://registry.npmjs.org/`; caller project/user config and non-allowlisted `npm_config_*` material cannot redirect it. The no-network wrapper observed all three exact package argv/cwd/config boundaries, asserted only credential/OIDC key presence without reading values, and remained uninvoked for hostile environment/project redirects.
- Secret/scope finding: the only workflow `secrets.NPM_TOKEN` reference is `NODE_AUTH_TOKEN` on the exact publication step at `.github/workflows/ci.yml:137-140`; OIDC write permission remains confined to the publish job. Current implementation/tests do not print token values, and focused test wrappers inspect required credential key presence only. No root/CLI target or non-npmjs mutation route was found.
- Required remediation: none.

## Rework Execution Result Round 6 — executor-task03-nonreplayable-history-20260810

- Bounded correction: remediated only Change Review Round 2 BF-1/confirmed CH-1 in `scripts/release-eligibility.mjs`, the eligibility workflow checkout, its real disposable-history regression, and this appended evidence. Publication, provenance, package sources/candidate bytes, prior evidence/reviews/challenges/drift, and SDD state were not changed.
- Complete-history authorization: eligibility still requires a push to `refs/heads/master`, an exact lowercase 40-hex supplied SHA, and exact current marker content. It now additionally requires Git to report a non-shallow repository, requires the supplied SHA to equal checked-out `HEAD`, traverses reachable objects with missing objects treated as errors, and enumerates all marker additions through the complete reachable HEAD ancestry using full-history `git log --diff-filter=A`. Eligibility is true only when that enumeration contains exactly one addition and it is the supplied SHA; history-command failure, missing history, shallow checkout, non-HEAD SHA, malformed/deleted marker, multiple additions, or any other result fails closed before artifact or Registry work.
- Workflow boundary: the eligibility checkout now uses `fetch-depth: 0`, and the workflow regression asserts that exact value. Candidate retention and publication remain dependent on the eligibility output, so ancestry validation completes before candidate recovery/generation/upload, Registry lookup, or mutation. Same-SHA reruns, fixed non-cancelling concurrency, exact SHA/run-ID artifact recovery, master-only event policy, and validation-only PR behavior remain intact.
- Real history matrix: the disposable Git regression constructs base → exact add A → inherited later B → semantic marker modification → deletion → exact re-add C. It evaluates C before B before A and records only A as reaching modeled Registry operations; A succeeds twice at the same checked-out SHA. B, modification, deletion, C re-add, PR, non-master ref, and a supplied non-HEAD SHA cannot become eligible. A real depth-one `file://` clone and a directory with unavailable Git history both fail closed.
- TDD RED: pinned `node --test --test-name-pattern='unique complete-history' scripts/release-packages.test.mjs` exited `1` (`0/1`, `0.38s`); decisive output showed actual modeled operations `["C","A"]` rather than `["A"]`, directly reproducing replay eligibility. The first post-implementation attempt then exposed a test-fixture portability issue because local `git init` selected `main`; explicitly creating the modeled `master` ref corrected the shallow-clone setup without changing production behavior.
- GREEN/current checks: final focused eligibility/workflow plus eligibility syntax exited `0` (`2/2`, `0.83s`). Final pinned `npm run release:test` exited `0` (`13/13`, `24.76s`). Deterministic release build retained candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; release verification exited `0` with inventories `63/103/91`, one `zod@3.25.76`, the required dependency graph, and 12 runtime/12 type entries. Typecheck and eligibility/publisher/Registry-smoke syntax checks exited `0`.
- Mandatory/residue checks: pinned `npm ci`, lint, build, and test ran in required order and exited `0`; dependency architecture was valid, Vitest reported `139` files / `1095` tests, and npm reported the unchanged 17 audit findings. Package smoke printed version `0.8.0` and help and exited `0`. `git diff --check` and empty staging exited `0`; no credential, external Git/GitHub, Registry, commit, push, or publish operation occurred.
- Round identities: `scripts/release-eligibility.mjs` is `sha256:9cc5e1550f2d7f294c79fb77087cdd1d540ad7f4c04e1623ec78bd998925f197`; `scripts/release-packages.test.mjs` is `sha256:9029718c7f9b88d0e2c684b892e542a929c5eccf394ee597536148ca6ff5afd8`; `.github/workflows/ci.yml` is `sha256:8f12caa1bac776e216b92bf1d91e06c9ee2e5ed21a50f5390e16c04333b34219`.

## Executor Self-check Round 6 — executor-task03-nonreplayable-history-20260810

- Non-replayability: an exact marker re-add can no longer mint a second release authorization because every reachable marker addition is counted; only the unique original addition at current HEAD qualifies. Immediate-parent status is no longer the authorization proof.
- Completeness/fail-closed behavior: the script rejects shallow repositories explicitly, rejects SHA/HEAD disagreement, and requires reachable-object and full-history traversal to succeed. The workflow supplies full history, while shallow/unavailable fixtures demonstrate that missing proof cannot degrade into eligibility.
- Ordering and retry safety: reversed readiness admits only A to modeled Registry operations; later, modified, deleted, and re-added states are excluded. Re-evaluating the same A checkout yields the same eligible result, preserving same-run retry/recovery behavior.
- Scope/handoff: all focused, complete release, deterministic candidate, consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue evidence is current. Only Round 6-authorized TASK-03 paths changed, immutable records remain intact, and no executor verdict was written.

## Independent Quick Review Round 6 — reviewer-task03-quick-r6-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; Change Review Round 2 sole blocker BF-1/confirmed CH-1; Rework Execution/Self-check Round 6; directly affected RQ-009, RQ-017, RQ-018 and Plan accepted-result/master-push eligibility, same-SHA retry, validation-only, and fixed-concurrency boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/release-eligibility.mjs` (`sha256:9cc5e1550f2d7f294c79fb77087cdd1d540ad7f4c04e1623ec78bd998925f197`), complete-history eligibility/workflow regressions in `scripts/release-packages.test.mjs` (`sha256:9029718c7f9b88d0e2c684b892e542a929c5eccf394ee597536148ca6ff5afd8`), `.github/workflows/ci.yml` (`sha256:8f12caa1bac776e216b92bf1d91e06c9ee2e5ed21a50f5390e16c04333b34219`), and unchanged exact marker `scripts/release-intent-0.8.0.json` (`sha256:075ea0d2fdddbb409eff26c904f397d013cd9ed3f036022c7340d536f534535f`).
- Commands/evidence inspected: current sole blocker, Round 6 RED/GREEN/full/integration/gate/residue evidence, eligibility script, marker, workflow gates, and disposable-history regression. Reviewer freshly ran the complete-history eligibility plus workflow cases (2/2 passed), eligibility syntax check, complete diff whitespace validation, empty-staging validation, workflow gate scan, and exact identity hashes. No live lookup, credential access, external mutation, task-worktree commit, push, or publish occurred.
- Finding disposition: BF-1/CH-1 is resolved. `scripts/release-eligibility.mjs:29-63` requires the exact master-push event/ref, lowercase 40-hex supplied SHA, non-shallow repository, supplied SHA equal checked-out `HEAD`, exact marker contract, successful missing-object traversal, and full reachable-history enumeration of marker additions. Eligibility is true only when exactly one reachable addition exists and its commit equals the supplied/current SHA; history/object failure is fatal rather than an ineligible approximation.
- Regression completeness: `scripts/release-packages.test.mjs:665-755` constructs base → add A → inherited B → modified marker → deletion → exact re-add C, evaluates C and B before A, and observes only A in modeled Registry operations. A is eligible twice at the same checked-out SHA; B is cleanly ineligible, while modification, deletion, C replay, PR, non-master ref, non-HEAD SHA, real depth-one clone, and unavailable Git history all fail closed. This directly covers replay and reversed-readiness behavior rather than only static workflow shape.
- Workflow finding: `.github/workflows/ci.yml:31-53` fetches full history with exact `fetch-depth: 0`, evaluates eligibility after repository checks and before candidate work, and exports the decision. Candidate creation and publication remain gated by that exact output at `.github/workflows/ci.yml:50-53,105-108`, so every excluded state reaches neither candidate recovery/generation nor Registry lookup/mutation; fixed non-cancelling concurrency and same-SHA artifact recovery remain unchanged.
- Required remediation: none.

## Rework Execution Result Round 7 — executor-task03-registry-npm-config-20260810

- Bounded correction: remediated only Change Review Round 3 BF-1/confirmed CH-2 in `scripts/registry-smoke.mjs`, its exact npm-child regression, and this appended evidence. Eligibility, publication, provenance semantics, workflow, package sources/candidate bytes, prior evidence/reviews/challenges/drift, and SDD state were not changed.
- Owned npm environment: Registry-smoke now builds npm child environments from an explicit runtime allowlist rather than copying caller state. Every case variant of every inherited `npm_config_*` key—including duplicate Registry aliases, user/global/cache, proxy, lifecycle/tag, auth/token/OTP, certificate, and other material—is absent by construction; caller proxy and credential variables outside the runtime allowlist are also excluded.
- Single authority/config boundary: each run creates canonical owned consumer/config roots, distinct empty user/global configs, and an owned cache. The only injected npm keys are those three paths plus exact `NPM_CONFIG_REGISTRY=https://registry.npmjs.org/`. Npm runs from the owned consumer cwd with no project `.npmrc`; user/global discovery is redirected to the owned empty files.
- Exact wrapper fixture: fixture-only `--npm-command-fixture` requires the credential-free Registry response fixture and uses the same install/`npm ls` path and exact child environment. The no-network wrapper observes both calls and proves exactly four uppercase owned npm keys, no aliases, canonical npmjs, empty configs, owned cache/cwd, no credential-shaped keys, and exact engine/extensions specs. It supplies deterministic tree/core evidence; the workflow's non-fixture path remains unchanged through real Registry install, automatic core/one-Zod, runtime, and NodeNext type checks.
- TDD RED: pinned focused test exited `1` (`0/1`, `1.05s`), with wrapper exit `91` after observing inherited conflicting mixed-case npm configuration. TDD GREEN: Registry-smoke syntax plus the identical focused test exited `0` (`1/1`, `0.10s`). Final pinned release suite exited `0` (`14/14`, `26.52s`).
- Integrated/current gates: deterministic build retained candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; release verification exited `0` with inventories `63/103/91`, one `zod@3.25.76`, required graph, and 12 runtime/12 type entries. Typecheck and all release-script syntax checks exited `0`. Pinned `npm ci`, lint, build, and test exited `0` in order (`139` files / `1095` tests; unchanged 17 audit findings). Package smoke, whitespace, and empty staging passed.
- Round identities/residue: Registry-smoke is `sha256:617dd31d162906a90de701db6e4e77b3e85d5c09debaef0e9bd98023b0dc0bb2`; tests are `sha256:d7f11f3ff6e4c27fa19617e42197f22863ede7ae8f8e92e5d55437365dbe223c`. Executable scan finds only the four owned `NPM_CONFIG_*` assignments. No credential value, live network/Registry request, GitHub operation, commit, push, publish, or external mutation occurred.

## Executor Self-check Round 7 — executor-task03-registry-npm-config-20260810

- BF-1/CH-2: environment insertion order cannot select a hostile alias because no caller npm configuration key is copied. Install and dependency-tree npm subprocesses receive exactly one Registry authority and the same owned boundary.
- Registry-only integrity: metadata/provenance remains credential-free and npmjs-only; the live consumer still requests exact engine/extensions versions and proves automatic core, no horizontal dependency, one Zod, and every runtime/type entry. Fixture mode requires an injected Registry fixture and is absent from the workflow.
- Config/credential isolation: cwd, configs, and cache are temporary and implementation-owned. No caller project/user config, npm proxy/cache/lifecycle value, mixed-case alias, or credential-shaped environment entry reaches npm; cleanup remains in `finally`.
- Scope/handoff: focused, complete release, deterministic candidate, consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue evidence is current. Only authorized Round 7 paths changed; immutable records remain intact and no executor verdict was written.

## Independent Quick Review Round 7 — reviewer-task03-quick-r7-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; Change Review Round 3 sole blocker BF-1/confirmed CH-2; Rework Execution/Self-check Round 7; directly affected SC-7, RQ-014/RQ-015, AC-007/AC-011, and Plan BD-008/T-009/CK-018 Registry-only consumer and credential/config-isolation boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/registry-smoke.mjs` (`sha256:617dd31d162906a90de701db6e4e77b3e85d5c09debaef0e9bd98023b0dc0bb2`) and exact npm-child regression in `scripts/release-packages.test.mjs` (`sha256:d7f11f3ff6e4c27fa19617e42197f22863ede7ae8f8e92e5d55437365dbe223c`).
- Commands/evidence inspected: current sole blocker, Round 7 RED/GREEN/full release/exact-candidate/mandatory/residue evidence, current Registry-smoke npm environment and consumer path, and the exact no-network wrapper regression. Reviewer freshly ran the focused Registry-consumer npm-isolation case (1/1 passed), Registry-smoke syntax check, repository typecheck, complete diff whitespace validation, empty-staging validation, executable npm-config assignment scan, and exact file identities. No credential access, live Registry request, external mutation, commit, push, or publish occurred.
- Finding disposition: BF-1/CH-2 is resolved. `npmEnvironment` at `scripts/registry-smoke.mjs:33-46` constructs a new environment from an exact generic runtime allowlist; it never copies caller npm configuration and therefore removes every case variant and duplicate form of inherited `npm_config_*`/`NPM_CONFIG_*`, including Registry, config-path, cache, proxy, lifecycle/tag, certificate, auth/token/OTP, and other material. It then adds exactly four uppercase owned keys: distinct user/global config paths, canonical `https://registry.npmjs.org/`, and owned cache.
- Child-boundary evidence: `scripts/registry-smoke.mjs:333-363` creates canonical owned consumer/config roots, writes empty user/global configs, creates an owned cache, and runs both install and `npm ls` from the owned consumer cwd with the same rebuilt environment. The wrapper regression at `scripts/release-packages.test.mjs:567-648` injects conflicting upper/lower Registry aliases plus hostile user/global/cache/proxy/tag/auth/token/OTP state, observes both npm calls, and proves exactly the four expected uppercase npm keys, canonical npmjs, empty owned configs, owned cache/cwd, no credential-shaped keys, and exact engine/extensions install specs.
- Consumer-preservation finding: fixture-only execution returns only after the same install/tree/core assertions at `scripts/registry-smoke.mjs:358-382`; the non-fixture path then remains intact at `scripts/registry-smoke.mjs:384-409`, proving one physical Zod resolution and executing every canonical public entry through runtime imports and strict NodeNext TypeScript. The executor's fresh complete release suite (14/14) and exact-candidate verification (63/103/91 inventories, one `zod@3.25.76`, 12 runtime/12 type entries) provide current integrated evidence that the isolation change did not regress consumer behavior.
- Required remediation: none.

## Rework Execution Result Round 8 — executor-task03-oidc-pair-20260810

- Bounded correction: remediated only Change Review Round 4 BF-1/confirmed CH-1 in the publisher environment/preflight, its exact no-network npm-child regression, and this appended evidence. Eligibility, Registry-smoke, publication state/order/identity, workflow permissions/token scope, package sources/candidate bytes, prior records, and SDD state were not changed.
- Exact child allowlist: the publisher's required-key set now contains exactly `NODE_AUTH_TOKEN`, `ACTIONS_ID_TOKEN_REQUEST_URL`, and `ACTIONS_ID_TOKEN_REQUEST_TOKEN`. Required-key admission occurs before generic credential-shaped filtering, so the full Actions OIDC request pair survives while unrelated `TOKEN`/credential keys—including `GITHUB_TOKEN`—remain excluded. Ordinary nonsecret GitHub/runner runtime context remains allowlisted; owned npm configs and canonical Registry injection are unchanged.
- Live provenance preflight: before Registry client construction or classification/mutation, non-dry-run live publication requires all three nonempty inputs. A credential-bearing npm-command fixture enforces the same complete triplet, permitting exact no-network observation of the live boundary. Credential-free fixtures remain allowed for state-machine tests, and dry-run remains credential-free regardless of caller inputs. Failure reports only missing key names, never values.
- No-network matrix: with all three synthetic inputs, the wrapper receives exact retained-tarball/provenance/access/registry argv for all three packages, both OIDC pair members, auth, owned cwd/config, and no unrelated/GitHub token keys. Separate URL-missing, request-token-missing, and auth-missing runs use an intentionally invalid Registry fixture yet fail with publication-credential diagnostics before that fixture can be parsed and before any wrapper call. Final wrapper call count remains exactly three; values are neither logged nor asserted.
- TDD RED: pinned focused publisher-boundary test exited `1` (`0/1`, `0.77s`); the exact child wrapper exited `95` because the Actions request URL was absent, reproducing the review probe. TDD GREEN: publisher syntax plus the identical focused test exited `0` (`1/1`, `3.74s`). Final pinned release suite exited `0` (`14/14`, `27.23s`).
- Integrated/current gates: deterministic build retained candidate `sha256:ce79e84dc3f62f86041f34580a0a9d08c0aec21b757e4c3c95bb30299d9b6604`; exact release verification exited `0` with inventories `63/103/91`, one `zod@3.25.76`, required graph, and 12 runtime/12 type entries. Typecheck and all release-script syntax checks exited `0`. Pinned `npm ci`, lint, build, and test exited `0` in order (`139` files / `1095` tests; unchanged 17 audit findings). Package smoke, whitespace, and empty staging passed.
- Round identities/residue: publisher is `sha256:1636a4ca7c988e6971265eeab83288dedad0079d91c3b0efad3dc162675a7da0`; tests are `sha256:8fa024c43fb395d516d58d49577858aa28bfae5444498c48bef90e3337e1f69b`. Scoped scan shows only the three intended publication/OIDC key names and literal npm token mapping; no values, credentials, live Registry/GitHub request, commit, push, publish, or external mutation occurred.

## Executor Self-check Round 8 — executor-task03-oidc-pair-20260810

- BF-1/CH-1: explicit required-key handling precedes credential rejection, so the Actions URL can no longer be discarded because its name contains `TOKEN`. Both pair members and npm auth reach only the exact publication child.
- Fail-closed order: live or credential-bearing simulated provenance publication cannot construct the Registry client or invoke npm with an incomplete pair/auth triplet. Dry-run and fully credential-free fixtures retain their approved behavior.
- Isolation and non-disclosure: generic GitHub/runner context remains available, but unrelated credential-shaped keys are excluded. Tests observe only key presence/absence and command/config identity; neither implementation nor wrapper logs synthetic values.
- Scope/handoff: focused, complete release, deterministic candidate, consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue evidence is current. Only authorized Round 8 paths changed; immutable records remain intact and no executor verdict was written.

## Independent Quick Review Round 8 — reviewer-task03-quick-r8-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; Change Review Round 4 sole blocker BF-1/confirmed CH-1; Rework Execution/Self-check Round 8; directly affected provenance publication credential admission, pre-Registry fail-closed ordering, dry-run/fixture compatibility, and workflow OIDC/token-scope boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `scripts/publish-release-packages.mjs` (`sha256:1636a4ca7c988e6971265eeab83288dedad0079d91c3b0efad3dc162675a7da0`), exact npm-child regression in `scripts/release-packages.test.mjs` (`sha256:8fa024c43fb395d516d58d49577858aa28bfae5444498c48bef90e3337e1f69b`), and unchanged publication workflow `.github/workflows/ci.yml` (`sha256:8f12caa1bac776e216b92bf1d91e06c9ee2e5ed21a50f5390e16c04333b34219`).
- Commands/evidence inspected: current final-review blocker, Round 8 RED/GREEN/full release/candidate/gate/residue evidence, current publisher, exact no-network wrapper matrix, and workflow publication job. Reviewer freshly ran the focused Registry/publication-boundary regression (1/1 passed), publisher syntax check, a fully credential-free retained-candidate dry-run (exit 0; exactly three core/engine/extensions commands with exact tarballs and `--provenance --access public --registry https://registry.npmjs.org/`), complete diff whitespace validation, empty-staging validation, and exact identities. The first standalone dry-run attempt used a nonexistent `.release-candidate/v0.8.0` path and failed at candidate `realpath`; rerunning against the recorded `.release/candidate/manifest.json` produced the cited successful evidence. No credential value was read or logged, and no live Registry/GitHub request, external mutation, commit, push, or publish occurred.
- Finding disposition: BF-1/CH-1 is resolved. `publicationEnvironment` at `scripts/publish-release-packages.mjs:61-86` admits exactly `NODE_AUTH_TOKEN`, `ACTIONS_ID_TOKEN_REQUEST_URL`, and `ACTIONS_ID_TOKEN_REQUEST_TOKEN` before the generic credential-key rejection, retaining the complete OIDC pair while excluding unrelated credential-shaped keys. Owned npm config paths and the single canonical npmjs Registry remain injected independently.
- Fail-closed and compatibility finding: `assertPublicationCredentials` at `scripts/publish-release-packages.mjs:89-101` requires the complete nonempty trio for live publication and for any credential-bearing fixture, while allowing dry-run and fully credential-free fixtures. It runs at line 280 before publication-context creation, Registry client construction/fixture parsing at line 285, lookup, classification, or mutation. The regression at `scripts/release-packages.test.mjs:840-924` proves all three keys reach the exact publish child with exact argv/cwd/config, unrelated `UNRELATED_TOKEN`/`GITHUB_TOKEN` do not, and omission of each trio member independently fails against an intentionally invalid Registry fixture before lookup or wrapper invocation; the wrapper call count remains exactly three.
- Workflow finding: `.github/workflows/ci.yml:105-140` retains job-level `id-token: write` and scopes `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}` to the sole publication step. The OIDC request URL/token are supplied by the Actions OIDC runtime under that permission and are not manually broadened to other steps; no unrelated credential environment is added.
- Required remediation: none.

## Rework Execution Result Round 9 — executor-task03-pr-package-validation-20260810

- Bounded correction: remediated only Change Review Round 5 BF-2 in `.github/workflows/ci.yml`, its parsed job-graph regression, and this appended evidence. TASK-01 repository projection, release scripts, publication/Registry behavior, eligibility semantics, package sources/candidate bytes, prior records, and SDD state were not changed.
- Credential-free package gate: added unconditional `package-validation`, running in parallel with the normal matrix check on every `pull_request` and master push. It checks out source, pins exact Node `22.22.0` and npm `10.9.4` with explicit assertions, runs `npm ci` and build, generates the deterministic candidate with `release:build -- --verify-determinism`, executes the complete `release:test`, then exact `release:verify` against `.release/candidate/manifest.json`. This covers package contract, directory/tarball identity and inventory, strict-peer clean consumer, automatic core/one-Zod graph, and all 12 runtime plus 12 type entries.
- Nonmutation boundary: the package-validation job has no event condition, job permission override, artifact upload/download, release retention record, Registry smoke/lookup, publish command, OIDC permission, secret reference, or token environment. It inherits only workflow `contents: read`; generated `.release` data stays runner-local and ephemeral.
- Master dependency graph: `release-eligibility` now needs both ordinary `check` and `package-validation`; `release-candidate` needs those plus eligibility; `publish` needs all three plus the retained-candidate job. Thus eligible master mutation depends on the same successful acceptance, while PR and later/noneligible master runs still validate packages and then skip retention/upload/Registry mutation through the unchanged marker eligibility result.
- Parsed event regression: the workflow test requires the exact package-validation toolchain and command sequence, absence of artifact/publish/Registry/secret/token behavior, and exact dependency arrays. Its PR evaluation identifies only `check`, `package-validation`, and read-only `release-eligibility` as runnable and scans that graph for no upload, publish, NPM secret, or OIDC permission; eligible publication requires package validation through direct job dependencies.
- TDD RED: pinned workflow-focused test exited `1` (`0/1`, `0.04s`) with decisive `pull requests do not run package validation`. TDD GREEN: the identical parsed-workflow test exited `0` (`1/1`, `0.04s`). Final pinned release suite exited `0` (`14/14`, `31.72s`).
- Integrated/current gates: deterministic build and exact release verification exited `0` with current repository-bearing candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, inventories `63/103/91`, one `zod@3.25.76`, required graph, and 12 runtime/12 type entries. Typecheck and all release-script syntax checks exited `0`. Pinned `npm ci`, lint, build, and test exited `0` in order (`139` files / `1095` tests; unchanged 17 audit findings). Package smoke, whitespace, and empty staging passed.
- Round identities/residue: workflow is `sha256:c88954d1f28b13f5f80e58958df9fa03793d98bcff0b7a6cc2898b10961f789d`; integrated tests are `sha256:cf4a8bd29b3d234ee7d1ac362015b2433014161fc1ea97c93f4adb04c5cde87c`. Scoped workflow scan confirms the sole NPM secret and `id-token: write` remain only in the gated publish job; no credentials, external request, GitHub action execution, commit, push, publish, or Registry mutation occurred.

## Executor Self-check Round 9 — executor-task03-pr-package-validation-20260810

- BF-2 closure: pull requests can no longer pass repository-only checks while skipping package acceptance. They execute deterministic generation, the full release suite, and exact packed-consumer verification under the release toolchain.
- Validation/mutation separation: package-validation is unconditional and credential-free; retention and mutation remain controlled by exact master marker eligibility. PR and noneligible master events validate but cannot upload a release candidate or enter publication.
- Same acceptance authority: eligibility, candidate retention, and publish all require successful package-validation; eligible master cannot bypass the exact package result validated on PR/master. Existing retry recovery may still reaccept/regenerate its retained same-SHA candidate as designed.
- Scope/handoff: focused workflow, complete release, deterministic candidate/consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue evidence is current. Only authorized Round 9 TASK-03 paths changed, immutable records remain intact, and no executor verdict was written.

## Independent Quick Review Round 9 — reviewer-task03-quick-r9-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; Change Review Round 5 BF-2; Rework Execution/Self-check Round 9; directly affected RQ-009, Plan BD-006/T-005 step 1/CK-009, unconditional PR/master package acceptance, and retention/publication eligibility boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `.github/workflows/ci.yml` (`sha256:c88954d1f28b13f5f80e58958df9fa03793d98bcff0b7a6cc2898b10961f789d`) and parsed workflow/eligibility regression in `scripts/release-packages.test.mjs` (`sha256:cf4a8bd29b3d234ee7d1ac362015b2433014161fc1ea97c93f4adb04c5cde87c`). Publication, eligibility, Registry-smoke, release scripts, and package candidate implementation were unchanged in this round.
- Commands/evidence inspected: Change Review Round 5 BF-2, Round 9 RED/GREEN/full/integration/gate evidence, current workflow, parsed job-graph assertions, and unchanged complete-history eligibility regression. Reviewer freshly ran the workflow plus unique marker-history cases with all publication/OIDC variables removed (2/2 passed), the complete credential-cleared release suite (14/14 passed), exact retained-candidate verification (exit 0; candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, inventories `63/103/91`, one Zod, 12 runtime/12 type entries), complete diff whitespace validation, and empty-staging validation. No credentials, workflow execution, external request/mutation, commit, push, publish, or Registry operation occurred.
- Finding disposition: BF-2 is resolved. `.github/workflows/ci.yml:31-52` defines `package-validation` without `if` or `needs`, so it runs for both configured pull requests and master pushes. Under inherited workflow `contents: read`, it pins and asserts Node `22.22.0`/npm `10.9.4`, runs `npm ci`, `npm run build`, deterministic `release:build -- --verify-determinism`, the complete `release:test`, and exact `release:verify` against the generated retained-candidate shape.
- PR nonmutation finding: the unconditional job contains no artifact upload/download or recovery, publication, Registry smoke/lookup, secret/token environment, OIDC permission, or job-level permission expansion; its `.release` output remains runner-local. For pull requests, the parsed graph at `scripts/release-packages.test.mjs:999-1047` admits only `check`, `package-validation`, and read-only `release-eligibility`, scans those jobs for no upload/publication/npm secret/OIDC authority, and proves both `release-candidate` and `publish` remain behind the false eligibility output. Workflow-level authority is only `contents: read`.
- Master and marker finding: `release-eligibility` at `.github/workflows/ci.yml:54-71` now needs successful `check` plus `package-validation`; retained candidate at lines 73-126 additionally needs the exact eligibility result, and `publish` at lines 128-165 needs all of those plus successful retention. Both mutation-capable jobs retain `if: needs.release-eligibility.outputs.eligible == 'true'`. The unchanged complete-history regression freshly passed, confirming only the unique current master commit adding the exact marker is eligible, same-SHA retry remains eligible, and later/modified/deleted/re-added/PR/non-HEAD/shallow states skip retention and publication.
- Required remediation: none.

## Rework Execution Result Round 10 — executor-task03-hidden-candidate-artifact-20260810

- Bounded correction: remediated only Change Review Round 6 BF-1 in the candidate upload configuration, its parsed workflow regression, and this appended evidence. Package-validation, TASK-01 repository projection, release scripts, eligibility/publication/Registry behavior, candidate bytes, prior records, and SDD state were not changed.
- Narrow hidden-file opt-in: the sole `actions/upload-artifact@v4` step still targets exactly `.release/candidate`, retains `if-no-files-found: error`, and now sets `include-hidden-files: true`. No workspace/root path, other job, download, or other step has this opt-in. This includes files beneath hidden `.release` without broadening retention beyond the accepted candidate tree.
- Fail-closed pre-upload checks: the immediately preceding residue step now explicitly requires `.release/candidate/manifest.json`, rejects any candidate `.npmrc`, PEM, or key file, and requires exactly three regular `.tgz` files under `.release/candidate/archives`. It remains after complete candidate reacceptance/run-record creation and before upload; hidden-file inclusion therefore cannot silently retain forbidden config/secret residue.
- Download/mutation ordering: parsed steps require the retained artifact to download into exact `.release/candidate`; `release:verify -- --candidate .release/candidate/manifest.json` must follow download and precede exact publish, which precedes retained-candidate Registry smoke. Thus the hosted-artifact fix does not weaken reacceptance or allow mutation before verified retention.
- Parser regression: tests find exactly one upload action across the workflow, require exact candidate path, boolean hidden opt-in, error-on-empty policy, and reject broad `.` selection. They prove the hidden opt-in appears only on that upload, inspect manifest/residue/three-tarball commands and their pre-upload ordering, and compare parsed download/reverify/publish/smoke indices and paths.
- TDD RED: pinned workflow-focused test exited `1` (`0/1`, `0.04s`); decisive mismatch was actual `include-hidden-files === undefined` versus required `true`, reproducing the hosted action default exclusion. TDD GREEN: the identical test exited `0` (`1/1`, `0.04s`). Final pinned release suite exited `0` (`14/14`, `27.15s`).
- Integrated/current gates: deterministic build and exact release verification exited `0` with current candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, inventories `63/103/91`, one `zod@3.25.76`, required graph, and 12 runtime/12 type entries. Typecheck and all release-script syntax checks exited `0`. Pinned `npm ci`, lint, build, and test exited `0` in order (`139` files / `1095` tests; unchanged 17 audit findings). Package smoke, whitespace, and empty staging passed.
- Round identities/residue: workflow is `sha256:eb3bab115aff59a80076bf975aaed80e1edf4ee466bec77aaec3394026b4e288`; integrated tests are `sha256:79d6669803224b5339ffff7fb8186d77e5b464b5d4c2324436b197b6ec9b0e48`. Scoped workflow scan shows one hidden opt-in, one exact candidate upload, retained manifest/three-tarball/residue checks, and exact candidate download. No workflow execution, credentials, external request, commit, push, publish, or Registry/GitHub mutation occurred.

## Executor Self-check Round 10 — executor-task03-hidden-candidate-artifact-20260810

- BF-1/CH-1 closure: the v4 hidden-file default can no longer exclude the `.release` candidate tree, and empty/failed selection remains fatal. Opt-in scope is the exact already accepted candidate rather than the repository or workspace.
- Retention completeness/safety: a manifest and exactly three archives are required locally, forbidden hidden config/key material is rejected, and the upload occurs only afterward. Parser coverage protects all hosted-action inputs that caused the escape.
- Mutation safety: publication still downloads the named retained artifact, re-verifies the exact manifest and bytes, then publishes and smokes in order. Candidate upload remains a prerequisite job and occurs before any Registry mutation.
- Scope/handoff: focused workflow, complete release, deterministic candidate/consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, and residue evidence is current. Only authorized Round 10 TASK-03 paths changed, immutable records remain intact, and no executor verdict was written.

## Independent Quick Review Round 10 — reviewer-task03-quick-r10-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; Change Review Round 6 BF-1/confirmed CH-1; Rework Execution/Self-check Round 10; directly affected pre-mutation artifact retention, hidden-file selection, exact-candidate residue/inventory checks, and download/reacceptance ordering.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `.github/workflows/ci.yml` (`sha256:eb3bab115aff59a80076bf975aaed80e1edf4ee466bec77aaec3394026b4e288`) and parsed artifact-boundary regression in `scripts/release-packages.test.mjs` (`sha256:79d6669803224b5339ffff7fb8186d77e5b464b5d4c2324436b197b6ec9b0e48`). Candidate generator/verifier, eligibility, publisher, Registry smoke, and package bytes were unchanged in this round.
- Commands/evidence inspected: Change Review Round 6 sole blocker, Round 10 RED/GREEN/full/integration/gate evidence, current upload/download workflow, and parsed hosted-action regression. Reviewer freshly ran the credential-cleared focused workflow case (1/1 passed), complete release suite (14/14 passed), exact retained-candidate verification (exit 0; candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, inventories `63/103/91`, one Zod, 12 runtime/12 type entries), complete diff whitespace validation, and empty-staging validation. No hosted workflow, credential, external request/mutation, commit, push, publish, or Registry operation occurred.
- Finding disposition: BF-1/CH-1 is resolved. The workflow contains exactly one `actions/upload-artifact@v4` step at `.github/workflows/ci.yml:120-128`; it targets only `.release/candidate`, sets `include-hidden-files: true`, retains `if-no-files-found: error`, and does not broaden selection to the repository/workspace. No other step opts into hidden-file upload.
- Pre-upload safety finding: complete recovered/generated candidate reacceptance occurs at lines 107-110. The immediately preceding retention guard at lines 115-119 then requires the exact candidate manifest, rejects candidate-contained `.npmrc`, PEM, or key files, and requires exactly three regular `.tgz` files beneath `.release/candidate/archives` before upload. Together with the exact verifier and narrow upload root, hidden-file inclusion retains the accepted candidate tree without admitting a broader workspace boundary.
- Download/mutation finding: the publish job downloads the named SHA/run-ID artifact into exact `.release/candidate` at `.github/workflows/ci.yml:155-159`, runs exact `release:verify` on that manifest at lines 160-161, then invokes the exact publisher and only afterward Registry smoke at lines 162-167. The parsed regression at `scripts/release-packages.test.mjs:1031-1060` proves one narrow upload, explicit hidden opt-in/error-on-empty, pre-upload manifest/residue/three-archive guards, and ordered exact-path download → reverify → publish → smoke.
- Required remediation: none.

## Rework Execution Result Round 11 — executor-task03-immutable-action-pins-20260810

- Bounded correction: remediated only Change Review Round 7 BF-1/confirmed CH-1 in `.github/workflows/ci.yml`, its parsed workflow policy in `scripts/release-packages.test.mjs`, and this appended evidence. Candidate generation/verification, eligibility, publication, Registry smoke, package bytes, prior records/reviews/challenges/drift, and SDD state were not changed.
- Credential-free official ref resolution: with GitHub/npm token variables unset, prompting disabled, global/system Git config disabled, and credential helpers disabled, read-only `git ls-remote` against the four official `https://github.com/actions/*.git` repositories resolved `refs/tags/v4` on 2026-08-10 as: `actions/checkout` → `11d5960a326750d5838078e36cf38b85af677262`; `actions/setup-node` → `49933ea5288caeca8642d1e84afbd3f7d6820020`; `actions/upload-artifact` → `ea165f8d65b6e75b540449e92b4886f43607fa02`; `actions/download-artifact` → `d3f86a106a0bac45b974a628896c90dbdf5c8093`.
- Immutable workflow boundary: all 13 external `uses:` occurrences now select those exact full 40-lowercase-hex commits. Every line retains a trailing `# v4` tag comment for human maintenance context; no mutable tag, branch, or short SHA remains. Checkout/setup in ordinary validation, package validation, eligibility, retention, and publication are pinned, as are candidate recovery/upload and retained-candidate download.
- Parsed supply-chain regression: the workflow test enumerates every non-local `uses:` value, requires `owner/repo@<40 lowercase hex>`, and requires an exact allowlist mapping for the four approved official actions and resolved commits. Its negative matrix independently rejects a major tag, branch, short SHA, and an unapproved owner/repository; it also retains explicit upload/download action identity plus all existing gate, permission, artifact, hidden-file, credential, and mutation-order assertions.
- TDD RED: pinned focused `node --test --test-name-pattern='workflow retains' scripts/release-packages.test.mjs` exited `1` (`0/1`, `0.04s`); the decisive failure was `external action is not pinned to a full lowercase commit SHA: actions/checkout@v4`. TDD GREEN: after replacing every selector, the identical focused test exited `0` (`1/1`, `0.04s`).
- Complete/current gates: final pinned `npm run release:test` exited `0` (`14/14`, `31.26s`). Deterministic build retained candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`; exact verification exited `0` with inventories `63/103/91`, one `zod@3.25.76`, the required dependency graph, and 12 runtime/12 type entries. Typecheck and all five release-script syntax checks exited `0`.
- Mandatory/integrated gates: pinned `npm ci`, lint, build, and test ran in required order and exited `0`; dependency architecture was valid, Vitest reported `139` files / `1095` tests, and npm reported the unchanged 17 audit findings. Package smoke printed version `0.8.0` and help and exited `0`. `git diff --check`, empty staging, the exact external-action inventory, and the mutable/short-ref residue scan exited `0`.
- Round identities/safety: `.github/workflows/ci.yml` is `sha256:4faf39ec30d67156b70c0d0e3e4d66aad7e956731c144ed67b067dc9e694ddcf`; `scripts/release-packages.test.mjs` is `sha256:74cb1296035f179aae9400f36b2b3df6da343e9c3ff78a75a51b2d7d20255783`. The only external access was credential-free read-only official ref resolution; no credential was read/logged, and no workflow run, Registry request/mutation, commit, push, publish, or other external mutation occurred.

## Executor Self-check Round 11 — executor-task03-immutable-action-pins-20260810

- BF-1/CH-1 closure: every external component participating in validation, eligibility, candidate retention/recovery, and publication is now selected by reviewed immutable code rather than a movable tag. The exact allowlist prevents a syntactically full but substituted action repository or commit from passing the policy test.
- Regression strength: parsing covers all jobs and all external `uses:` steps rather than a selected release subset. Negative examples reach the format and allowlist branches, while upload/download identity checks and the existing hidden-candidate/download-reverification assertions ensure pinning did not accidentally replace the required artifact actions.
- Behavior preservation: action inputs, full-history checkout, pinned Node/npm toolchain, one-shot eligibility, exact candidate upload/download, core-first publication, step-scoped npm authentication, OIDC authority, and retained Registry smoke remain unchanged; only action selectors became immutable.
- Scope/handoff: focused, complete release, deterministic candidate/consumer, type/syntax, mandatory repository, package-smoke, whitespace, staging, immutable-ref, and residue evidence is current. Only authorized Round 11 TASK-03 paths changed, immutable records remain intact, and no executor verdict was written.

## Independent Quick Review Round 11 — reviewer-task03-quick-r11-20260810

- Verdict: `passed`.
- Reviewed contract and remediation: Task Unit Contract revision 1; Change Review Round 7 BF-1/confirmed CH-1; Rework Execution/Self-check Round 11; directly affected trusted-CI, exact accepted artifact, irreversible publication, credential/OIDC confinement, and external Action supply-chain boundaries.
- Owned diff identity: baseline/current `HEAD` `838cb8cb9b86046df1d3dac94702a48438c28692`; reviewed `.github/workflows/ci.yml` (`sha256:4faf39ec30d67156b70c0d0e3e4d66aad7e956731c144ed67b067dc9e694ddcf`) and external-action/workflow regression in `scripts/release-packages.test.mjs` (`sha256:74cb1296035f179aae9400f36b2b3df6da343e9c3ff78a75a51b2d7d20255783`). Candidate generation/verification, eligibility, publisher, Registry smoke, package bytes, and workflow inputs/ordering were unchanged in this round.
- Commands/evidence inspected: Change Review Round 7 sole blocker, Round 11 credential-free ref-resolution/RED/GREEN/full/gate evidence, current workflow, exact allowlist helper, and retained workflow-policy assertions. Reviewer independently ran credential-free read-only `git ls-remote` with prompting, global/system Git config, and credential helpers disabled against the four official `actions/*` repositories; current `refs/tags/v4` resolved exactly to checkout `11d5960a326750d5838078e36cf38b85af677262`, setup-node `49933ea5288caeca8642d1e84afbd3f7d6820020`, upload-artifact `ea165f8d65b6e75b540449e92b4886f43607fa02`, and download-artifact `d3f86a106a0bac45b974a628896c90dbdf5c8093`. Reviewer also freshly ran the credential-cleared focused workflow case (1/1 passed), complete release suite (14/14 passed), exact candidate verification (exit 0; candidate `sha256:257b3aa85865921409ed106ed53fdcc2fccdc61376fd9999c97f857ba3698d04`, inventories `63/103/91`, one Zod, 12 runtime/12 type entries), whitespace, and empty-staging checks. No credentials or external mutation were used.
- Finding disposition: BF-1/CH-1 is resolved. All 13 external `uses:` occurrences in `.github/workflows/ci.yml` select one of the four official allowlisted `actions/checkout`, `actions/setup-node`, `actions/upload-artifact`, or `actions/download-artifact` repositories at the independently resolved exact lowercase 40-hex commit. Every occurrence retains a readable trailing `# v4` comment; a scoped scan found 13 total uses, 13 full-SHA/commented uses, and no mutable major/branch selector, short SHA, uppercase/non-hex revision, local ambiguity, or unapproved owner/repository.
- Regression finding: `APPROVED_ACTION_PINS` and `assertApprovedExternalActionReferences` at `scripts/release-packages.test.mjs:16-42` require exact `owner/repo@<40 lowercase hex>` syntax plus the four-entry repository/commit mapping. The workflow test enumerates every non-local use and requires exactly 13 references at lines 1021-1025; its negative matrix rejects `@v4`, `@main`, a short SHA, and a full-SHA unapproved owner. Existing exact toolchain, package-validation, eligibility, concurrency/permissions, hidden candidate upload, exact download/reverification, sole secret, OIDC, publication, and smoke assertions all remain in the same passing test, providing current evidence that selector pinning did not alter workflow semantics.
- Required remediation: none.
